# IQRA LMS Frontend Correction Progress

| Phase | Status | Evidence |
|---|---|---|
| F00 | DONE | Baseline inspection complete. Found 96 lint errors, 33 typecheck errors, 25 test failures, build failures due to 'use client' placement, Playwright connection errors. `implementation_plan.md` created. |
| F01 | DONE | `pnpm generate:api` executed. Generated canonical `StudentList`, `StudentDetail`, `OrganizationInvitation` types in `src/lib/api/schema.d.ts`. Removed handwritten `StudentEnrollmentView` and `AddMemberPayload = any`. Replaced uncontracted membership POST with invitation API. Aligned all 12 feature API files to canonical OpenAPI types with zero unsafe casts. |
| F02 | DONE | Repositioned `'use client'` to line 1 in all 26 components. Implemented normalized error handling in `src/lib/api/errors.ts` and `src/lib/api/client.ts` preventing HTML leaks, handling 400, 401, 403, 404, 409, 5xx, and clearing tokens on 401. 7 error unit tests passed. |
| F03 | DONE | Token storage key unified to `auth_token` across all app code and e2e tests (zero `quran_fe_token` remaining). DRF token login/me/logout contract verified. Enabled dynamic global fetch in `apiClient`. `auth.test.ts` (2 tests) and `auth-provider.test.tsx` (6 tests) all passed. |
| F04 | DONE | Disentangled `User.role` ('lead' | 'sub' | 'student' | 'parent') from `OrganizationMembership.role` ('owner' | 'admin' | 'staff' | 'teacher'). Fixed mocks to adhere to OpenAPI roles. `config.test.ts` passed (3 tests). |
| F05 | DONE | Aligned capability matrix in `src/lib/permissions/capabilities.ts` with backend permissions (restricted `manage_staff`, `manage_students`, `manage_curriculum` to owner and admin). All 8 scenarios in `capabilities.test.ts` passed. |
| F06 | DONE | Centralized tenant query keys in `src/lib/api/query-keys.ts` with explicit `academyId` scoping. Verified tenant cache invalidation and tenant isolation via `academy-provider.test.tsx` (8 tests) and `tenant-isolation.test.tsx` (2 tests). |
| F07 | DONE | Replaced direct membership creation with backend invitation API workflow (`POST /api/organizations/{organization_pk}/invitations/` with `email` and `role`). Permission gate restricted to owner/admin. All 13 staff unit tests passed. |
| F08 | DONE | Canonical `StudentList`, `StudentDetail`, and `StudentEnrollmentCreate` integrated into `studentsApi` and components (`student-directory.tsx`, `student-detail.tsx`, `add-student-form.tsx`). Status display and selection bound to canonical `enrollment_status`. All 10 student unit tests passed. |
| F09 | DONE | Replaced count-guessing heuristics (`staff.length > 1`, etc.) with explicit step model backed by backend facts and endpoints. Marked uncontracted capabilities (Class Configuration, Notifications, Ready) truthfully as OPEN / NOT YET CONTRACTED ("Not available in the current academy setup"). All 13 onboarding unit tests passed. |
| F10 | DONE | Verified route architecture `/app/teachers`, `/app/teachers/add`, `/app/teachers/[memberId]`. Cleanly segregated Teacher management from Administrative staff in UI tabs and views. Eliminated all lingering `/app/staff` routing references across app and e2e specs. |
| F11 | DONE | Verified tenant-scoped scheduling endpoints using `{organization_pk}`. Segregated academy-wide finance management (`/app/finance`) and personal teacher statements (`/app/payouts`), gated by `view_academy_payouts` capability. Resolved track/level mismatch in `booking-form.tsx` and teacher string schema in `lead-payouts-view.tsx`. All 11 scheduling and payout tests passed. |
| F12 | DONE | Full verification complete with 100% clean passes: 1) `pnpm exec tsc --noEmit` passed with 0 errors. 2) `pnpm test run` passed with 36/36 test files and 147/147 tests passing. 3) `pnpm lint` passed with 0 errors and 0 warnings. 4) `pnpm build` passed with Turbopack, successfully generating all 25 routes. 5) `pnpm test:e2e` passed with 12/12 Playwright tests passing, verifying all 11 critical user journeys against the production Next.js build. |
