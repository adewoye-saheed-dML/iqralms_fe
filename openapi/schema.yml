openapi: 3.0.3
info:
  title: Quran Academy API
  version: 0.6.0
  description: 'Identity layer: users, roles, parent links, teacher profiles. Curriculum:
    tracks, levels, placement review. Scheduling: teacher availability, direct booking,
    Jitsi video. Routing: cohorts, capacity-based auto-assignment. Pricing: negotiated
    rates per student and level, lead-approved. Waitlist: preferred-teacher requests
    when that teacher is full. Assessment: per-session rubric scoring, lead review,
    family progress. Payouts: teacher payout records and period statements, lead-generated.
    Organizations: the multi-tenant foundation — academies and the memberships that
    grant access to one.'
paths:
  /api/accounts/me/:
    get:
      operationId: accounts_me_retrieve
      description: GET /api/accounts/me/ — the logged-in user's own profile.
      tags:
      - accounts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
  /api/accounts/my-children/:
    get:
      operationId: accounts_my_children_list
      description: GET /api/accounts/my-children/ — students linked to the calling
        parent.
      tags:
      - accounts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LinkedStudent'
          description: ''
  /api/accounts/organizations/{organization_pk}/children/:
    get:
      operationId: accounts_organizations_children_list
      description: |-
        GET /api/accounts/organizations/{id}/children/ — the caller's children *here*.

        The endpoint SaaS Phase 2 exists for. ``/my-children/`` answers "who are this
        parent's children", globally and correctly; this one answers "which of them is a
        student of *this* academy", and the difference between the two answers is the
        tenant boundary.

        Both sides must be active members: a parent whose own membership is suspended
        gets an empty list, and a linked child the academy has not admitted (or has
        suspended) is absent from it. So a real, global parent-child relationship cannot
        be used to make Academy A show a student who belongs to Academy B —
        ``tenancy.children_in_organization()`` is the one place that rule lives.

        A non-member is refused by ``IsOrganizationMember`` before the queryset runs,
        and a caller who is not a parent account gets the same 403
        ``/my-children/`` gives them. The fields are the same global ones too: no
        signup code, and nothing the academy owns.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - accounts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LinkedStudent'
          description: The caller's linked children who are active members of this
            organization. Empty when none are.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member of this organization, or not a parent
            account.
  /api/accounts/organizations/{organization_pk}/teacher-configurations/:
    get:
      operationId: accounts_organizations_teacher_configurations_list
      description: |-
        /api/accounts/organizations/{id}/teacher-configurations/ — owner/admin only.

        GET lists this academy's teaching terms; POST gives an existing member theirs.
        Staff and teacher members are refused both, the same narrower default the
        membership directory takes — what an academy pays its teachers is not something
        an ordinary member reads, and a narrow rule can be widened safely later.

        POST names a ``user``, and the membership is resolved inside this academy, so
        the request cannot reach a membership it does not administer. Only a ``lead`` or
        ``sub`` account may be given teaching terms: an organization role is authority,
        not a teaching identity, and Phase 2 preserves the account domain's rule about
        who can teach.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - accounts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrganizationTeacherConfiguration'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
    post:
      operationId: accounts_organizations_teacher_configurations_create
      description: |-
        /api/accounts/organizations/{id}/teacher-configurations/ — owner/admin only.

        GET lists this academy's teaching terms; POST gives an existing member theirs.
        Staff and teacher members are refused both, the same narrower default the
        membership directory takes — what an academy pays its teachers is not something
        an ordinary member reads, and a narrow rule can be widened safely later.

        POST names a ``user``, and the membership is resolved inside this academy, so
        the request cannot reach a membership it does not administer. Only a ``lead`` or
        ``sub`` account may be given teaching terms: an organization role is authority,
        not a teaching identity, and Phase 2 preserves the account domain's rule about
        who can teach.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - accounts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationTeacherConfigurationCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/OrganizationTeacherConfigurationCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/OrganizationTeacherConfigurationCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationTeacherConfiguration'
          description: ''
        '400':
          description: Not a member of this organization, already configured here,
            or an account that cannot teach.
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
  /api/accounts/organizations/{organization_pk}/teacher-configurations/{id}/:
    get:
      operationId: accounts_organizations_teacher_configurations_retrieve
      description: |-
        GET/PATCH one set of teaching terms — approve, re-cap the week, re-rate the hour.

        The endpoint that proves the phase: PATCHing a teacher's terms here changes what
        *this* academy asks of them and nothing about any other academy they work for,
        because the row being written belongs to one membership. A configuration id from
        another tenant is a 404 from the scoped queryset.

        PUT is not offered. A whole-object replace would have to accept ``membership``,
        which is not editable — terms pointing at a different person or academy are
        different terms.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - accounts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationTeacherConfiguration'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
        '404':
          description: No such teaching terms in this organization.
    patch:
      operationId: accounts_organizations_teacher_configurations_partial_update
      description: |-
        GET/PATCH one set of teaching terms — approve, re-cap the week, re-rate the hour.

        The endpoint that proves the phase: PATCHing a teacher's terms here changes what
        *this* academy asks of them and nothing about any other academy they work for,
        because the row being written belongs to one membership. A configuration id from
        another tenant is a 404 from the scoped queryset.

        PUT is not offered. A whole-object replace would have to accept ``membership``,
        which is not editable — terms pointing at a different person or academy are
        different terms.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - accounts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedOrganizationTeacherConfigurationUpdate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedOrganizationTeacherConfigurationUpdate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedOrganizationTeacherConfigurationUpdate'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationTeacherConfiguration'
          description: ''
        '400':
          description: Nothing to change, or a value out of range.
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
        '404':
          description: No such teaching terms in this organization.
  /api/accounts/parent-links/:
    post:
      operationId: accounts_parent_links_create
      description: POST /api/accounts/parent-links/ — parent links to a student by
        code.
      tags:
      - accounts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ParentLinkCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ParentLinkCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ParentLinkCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParentLinkCreate'
          description: ''
  /api/assessment/organizations/{organization_pk}/{id}/:
    get:
      operationId: assessment_organizations_retrieve
      description: |-
        GET /api/assessment/organizations/<organization_pk>/{id}/ — the lead opens one assessment in full.

        The drill-down from the review queue: booking, student, teacher, rubric
        snapshot, scores, flag reason and any review note. Lead-only, and reading it
        does **not** mark it reviewed — the spec is explicit that clearing a flag is an
        explicit action, so the review stamp only ever comes from the POST below.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeadAssessment'
          description: ''
  /api/assessment/organizations/{organization_pk}/{id}/review/:
    post:
      operationId: assessment_organizations_review_create
      description: |-
        POST /api/assessment/organizations/<organization_pk>/{id}/review/ — the lead annotates and marks reviewed.

        Writes three fields and no others. The teacher's scores, summary and flag are
        untouched, and the model refuses to change them on an existing row, so "review
        never rewrites historical teacher data" holds even for a caller that tries.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeadReview'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LeadReview'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LeadReview'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeadAssessment'
          description: Reviewed. The assessment has left the pending queue.
        '403':
          description: Not the lead teacher.
        '404':
          description: No such assessment.
  /api/assessment/organizations/{organization_pk}/bookings/{booking_id}/:
    post:
      operationId: assessment_organizations_bookings_create
      description: |-
        POST /api/assessment/organizations/<organization_pk>/bookings/{booking_id}/ — the teacher who taught it scores it.

        Two layers keep this to the right teacher and the right booking. The queryset
        is scoped to ``teacher=request.user``, so another teacher's session is a **404**
        rather than a 403 — a 403 would confirm the booking exists. And
        ``SessionAssessment.clean()`` re-checks ``assessed_by == booking.teacher``
        anyway, so a future caller that skips this view cannot get it wrong either.

        A booking that is scheduled, cancelled or a no-show is a **400** carrying the
        model's own message: it exists and belongs to this teacher, it is simply not a
        session that happened. Submitting an assessment does not mark a booking
        completed — that stays a separate act.
      parameters:
      - in: path
        name: booking_id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionAssessmentCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/SessionAssessmentCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SessionAssessmentCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherAssessment'
          description: Submitted, with the rubric and criterion names snapshotted.
        '400':
          description: The booking is not completed, the track has no active rubric,
            a criterion is missing/unknown/duplicated, a score is outside 1-5, or
            this booking is already assessed.
        '403':
          description: Not a teacher account.
        '404':
          description: No such session for this teacher.
  /api/assessment/organizations/{organization_pk}/child/:
    get:
      operationId: assessment_organizations_child_list
      description: |-
        GET /api/assessment/organizations/<organization_pk>/child/?student_id= — a linked child's assessment history.

        Same family shape as ``/mine/``, reached through the ``ParentLink`` check.
        Not in the spec's endpoint list, but the spec's visibility table gives a parent
        "linked-child assessments/progress" and progress alone would not be that.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: student_id
        schema:
          type: integer
        required: true
      - in: query
        name: track_id
        schema:
          type: integer
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FamilyAssessment'
          description: ''
        '403':
          description: Not a parent, or not this parent's child.
  /api/assessment/organizations/{organization_pk}/mine/:
    get:
      operationId: assessment_organizations_mine_list
      description: |-
        GET /api/assessment/organizations/<organization_pk>/mine/ — the student's own assessment history.

        Family shape: the teacher's summary, the criterion scores and comments, and no
        internal quality-control field at all. Scoped to ``student=request.user``, so
        this endpoint cannot be pointed at anybody else.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: track_id
        schema:
          type: integer
        description: Restrict to one track.
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FamilyAssessment'
          description: ''
  /api/assessment/organizations/{organization_pk}/progress/child/:
    get:
      operationId: assessment_organizations_progress_child_retrieve
      description: |-
        GET /api/assessment/organizations/<organization_pk>/progress/child/?student_id=&track_id= — a linked child's.

        The ``ParentLink`` check is the whole endpoint: an unrelated parent asking about
        somebody else's child is refused, and the refusal happens before any progress is
        computed, not by omitting fields from a response that was built anyway.
      parameters:
      - in: query
        name: from
        schema:
          type: string
        description: Inclusive ISO-8601 start of the period, by session date.
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: student_id
        schema:
          type: integer
        description: Which of your children.
        required: true
      - in: query
        name: to
        schema:
          type: string
        description: Exclusive ISO-8601 end of the period, by session date.
      - in: query
        name: track_id
        schema:
          type: integer
        required: true
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentProgress'
          description: ''
        '400':
          description: A missing or unknown parameter.
        '403':
          description: Not a parent, or not a parent of that student.
  /api/assessment/organizations/{organization_pk}/progress/mine/:
    get:
      operationId: assessment_organizations_progress_mine_retrieve
      description: GET /api/assessment/organizations/<organization_pk>/progress/mine/?track_id=
        — the student's own progress.
      parameters:
      - in: query
        name: from
        schema:
          type: string
        description: Inclusive ISO-8601 start of the period, by session date.
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: to
        schema:
          type: string
        description: Exclusive ISO-8601 end of the period, by session date.
      - in: query
        name: track_id
        schema:
          type: integer
        description: Which track's progress. Progress is per track.
        required: true
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentProgress'
          description: ''
        '400':
          description: track_id missing or unknown.
        '403':
          description: Not a student account.
  /api/assessment/organizations/{organization_pk}/reports/teachers/:
    get:
      operationId: assessment_organizations_reports_teachers_retrieve
      description: |-
        GET /api/assessment/organizations/<organization_pk>/reports/teachers/ — lead-only quality visibility.

        Optionally narrowed by ``from``/``to`` and ``track_id``. Per teacher: how many
        sessions they assessed, the overall average, the average per track, how many
        they flagged, and how many of those flags are still waiting on the lead.

        Two things it is not. It is **not** a leaderboard: rows come back ordered by
        username and carry no rank. And it does **not** invent rows — a teacher who
        assessed nothing in the window is absent rather than present with 0.00, because
        a missing assessment is missing data and a zero next to someone's name is an
        accusation.
      parameters:
      - in: query
        name: from
        schema:
          type: string
        description: Inclusive ISO-8601 start of the period, by session date.
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: to
        schema:
          type: string
        description: Exclusive ISO-8601 end of the period, by session date.
      - in: query
        name: track_id
        schema:
          type: integer
        description: Restrict to one track.
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TeacherReport'
          description: ''
        '400':
          description: An unparseable period or track_id.
        '403':
          description: Not the lead teacher — a sub-teacher has no academy-wide view.
  /api/assessment/organizations/{organization_pk}/review/queue/:
    get:
      operationId: assessment_organizations_review_queue_list
      description: |-
        GET /api/assessment/organizations/<organization_pk>/review/queue/ — flagged and not yet reviewed.

        One definition of the queue, ``SessionAssessment.pending_lead_review()``, so
        the count in the teacher report and the list here cannot disagree. A reviewed
        assessment leaves it; the flag itself stays on the record, because that the
        teacher raised one is history.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LeadAssessment'
          description: ''
  /api/assessment/organizations/{organization_pk}/rubrics/:
    get:
      operationId: assessment_organizations_rubrics_list
      description: |-
        /api/assessment/organizations/<organization_pk>/rubrics/ — the lead configures what teachers score against.

        * **GET ?track_id=** lists rubrics, newest first, superseded ones included:
          "what do we score Tajweed on, and what did we score it on before" is one
          question. Without ``track_id`` it lists every track's.
        * **POST** creates a rubric with its criteria. If the track already has an
          active rubric, that one is superseded rather than edited — see
          ``AssessmentRubricCreateSerializer``.

        Lead-only on both sides. A rubric decides what every teacher in the academy is
        measured on, so it is not something a sub-teacher configures for themselves.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: track_id
        schema:
          type: integer
        description: Restrict to one track's rubrics.
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AssessmentRubric'
          description: ''
        '403':
          description: Not the lead teacher.
    post:
      operationId: assessment_organizations_rubrics_create
      description: |-
        /api/assessment/organizations/<organization_pk>/rubrics/ — the lead configures what teachers score against.

        * **GET ?track_id=** lists rubrics, newest first, superseded ones included:
          "what do we score Tajweed on, and what did we score it on before" is one
          question. Without ``track_id`` it lists every track's.
        * **POST** creates a rubric with its criteria. If the track already has an
          active rubric, that one is superseded rather than edited — see
          ``AssessmentRubricCreateSerializer``.

        Lead-only on both sides. A rubric decides what every teacher in the academy is
        measured on, so it is not something a sub-teacher configures for themselves.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssessmentRubricCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AssessmentRubricCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AssessmentRubricCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssessmentRubric'
          description: Created and active. Any previous rubric for this track is now
            active=False, still queryable, and its criteria still back the assessments
            submitted against them.
        '400':
          description: Unknown track, no criteria, or a repeated order.
        '403':
          description: Not the lead teacher.
  /api/assessment/organizations/{organization_pk}/rubrics/{id}/:
    get:
      operationId: assessment_organizations_rubrics_retrieve
      description: |-
        /api/assessment/organizations/<organization_pk>/rubrics/{id}/ — read one rubric, or edit it in place.

        PATCH is the operation the historical-data rule is about: renaming or retiring
        a criterion changes what *future* assessments look like and leaves every
        existing one exactly as it was, because each score carries the criterion name
        it was submitted under. Nothing here rewrites an ``AssessmentScore``, and the
        model would refuse if it tried.

        PUT is not offered. A whole-object replace on live configuration whose
        criteria are referenced by historical records is not a coherent operation;
        replacing a rubric wholesale is a POST to the collection, which supersedes.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssessmentRubric'
          description: ''
    patch:
      operationId: assessment_organizations_rubrics_partial_update
      description: |-
        /api/assessment/organizations/<organization_pk>/rubrics/{id}/ — read one rubric, or edit it in place.

        PATCH is the operation the historical-data rule is about: renaming or retiring
        a criterion changes what *future* assessments look like and leaves every
        existing one exactly as it was, because each score carries the criterion name
        it was submitted under. Nothing here rewrites an ``AssessmentScore``, and the
        model would refuse if it tried.

        PUT is not offered. A whole-object replace on live configuration whose
        criteria are referenced by historical records is not a coherent operation;
        replacing a rubric wholesale is a POST to the collection, which supersedes.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedAssessmentRubricUpdate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedAssessmentRubricUpdate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedAssessmentRubricUpdate'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssessmentRubric'
          description: ''
        '400':
          description: A repeated order, or a criterion id from another rubric.
        '403':
          description: Not the lead teacher.
  /api/assessment/organizations/{organization_pk}/snapshots/:
    post:
      operationId: assessment_organizations_snapshots_create
      description: |-
        POST /api/assessment/organizations/<organization_pk>/snapshots/ — the lead freezes a period.

        An explicit lead action, deliberately: automatic snapshot jobs are out of scope
        for this phase. Repeating a request for the same student, track and period
        returns the **existing** row with a **200** and changes nothing, which is the
        spec's duplicate rule — a snapshot that moved when you asked for it twice would
        not be a snapshot.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProgressSnapshotCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ProgressSnapshotCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ProgressSnapshotCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgressSnapshot'
          description: Generated.
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgressSnapshot'
          description: This period was already snapshotted. The existing row is returned
            untouched — no duplicate, and no recomputation.
        '400':
          description: Not a student, an inverted period, or an unknown track.
        '403':
          description: Not the lead teacher.
  /api/assessment/organizations/{organization_pk}/snapshots/all/:
    get:
      operationId: assessment_organizations_snapshots_all_list
      description: |-
        GET /api/assessment/organizations/<organization_pk>/snapshots/?student_id=&track_id= — the lead's own view.

        Unpublished rows included: the lead generated them, and needs to see what is
        waiting to be released.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: student_id
        schema:
          type: integer
      - in: query
        name: track_id
        schema:
          type: integer
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProgressSnapshot'
          description: ''
  /api/assessment/organizations/{organization_pk}/snapshots/child/:
    get:
      operationId: assessment_organizations_snapshots_child_list
      description: GET /api/assessment/organizations/<organization_pk>/snapshots/child/?student_id=&track_id=
        — a linked child's.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: student_id
        schema:
          type: integer
        required: true
      - in: query
        name: track_id
        schema:
          type: integer
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FamilyProgressSnapshot'
          description: ''
        '403':
          description: Not a parent of that student.
  /api/assessment/organizations/{organization_pk}/snapshots/mine/:
    get:
      operationId: assessment_organizations_snapshots_mine_list
      description: GET /api/assessment/organizations/<organization_pk>/snapshots/mine/?track_id=
        — the student's published snapshots.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: track_id
        schema:
          type: integer
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FamilyProgressSnapshot'
          description: ''
  /api/assessment/organizations/{organization_pk}/teacher/mine/:
    get:
      operationId: assessment_organizations_teacher_mine_list
      description: |-
        GET /api/assessment/organizations/<organization_pk>/teacher/mine/ — what this teacher has submitted.

        Their own rows only, including their own flags. Not the lead's review notes:
        the spec gives a sub-teacher no access to those in this phase. The lead sees
        their own submissions here too — for academy-wide data they use the review
        queue and the report.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - assessment
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TeacherAssessment'
          description: ''
  /api/auth/login/:
    post:
      operationId: auth_login_create
      description: |-
        Check the credentials and return the REST Token
        if the credentials are valid and authenticated.
        Calls Django Auth login method to register User ID
        in Django session framework

        Accept the following POST parameters: username, password
        Return the REST Framework Token Object's key.
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Login'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Login'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Login'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Token'
          description: ''
  /api/auth/logout/:
    post:
      operationId: auth_logout_create
      description: |-
        Calls Django logout method and delete the Token object
        assigned to the current User object.

        Accepts/Returns nothing.
      tags:
      - auth
      security:
      - tokenAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestAuthDetail'
          description: ''
  /api/auth/register/:
    post:
      operationId: auth_register_create
      description: |-
        POST /api/auth/register/ — create a user.

        A minor student comes back with status ``pending_parent_link``: the account
        exists and can log in, but is not complete until a parent links to it.
      tags:
      - auth
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Register'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Register'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Register'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      - {}
      responses:
        '201':
          description: Created. Body carries the user, an account status of 'active'
            or 'pending_parent_link', and a human-readable detail message.
  /api/curriculum/organizations/{organization_pk}/levels/:
    get:
      operationId: curriculum_organizations_levels_list
      description: |-
        /api/curriculum/organizations/{id}/levels/ — the academy's ladders.

        Flat rather than nested under a track, with ``?track=<id>`` to narrow it: a
        client rendering an academy's whole curriculum wants one request, and the
        ownership check is no less clear for it because the track is validated against
        the route's academy either way.

        POST appends. The next ``order`` is computed server-side, so "insert at
        position 2" is not a request this API can express — which is how the Phase 2
        append-only rule survives the arrival of a write endpoint.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: track
        schema:
          type: string
        description: Only levels of this track. A track id from another academy returns
          an empty list.
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Level'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member of this organization.
    post:
      operationId: curriculum_organizations_levels_create
      description: |-
        /api/curriculum/organizations/{id}/levels/ — the academy's ladders.

        Flat rather than nested under a track, with ``?track=<id>`` to narrow it: a
        client rendering an academy's whole curriculum wants one request, and the
        ownership check is no less clear for it because the track is validated against
        the route's academy either way.

        POST appends. The next ``order`` is computed server-side, so "insert at
        position 2" is not a request this API can express — which is how the Phase 2
        append-only rule survives the arrival of a write endpoint.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LevelCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/LevelCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/LevelCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Level'
          description: Appended at the track's next order. The first level of a track
            is order 1.
        '400':
          description: No such track in this academy — including a valid track id
            owned by a different one.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
  /api/curriculum/organizations/{organization_pk}/levels/{id}/:
    get:
      operationId: curriculum_organizations_levels_retrieve
      description: GET/PATCH one level. Its name and its description, never its position.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Level'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member of this organization.
        '404':
          description: No such object in this organization. A valid id belonging to
            another academy answers the same way — the response never confirms that
            a row exists elsewhere.
    patch:
      operationId: curriculum_organizations_levels_partial_update
      description: GET/PATCH one level. Its name and its description, never its position.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedLevelUpdate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedLevelUpdate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedLevelUpdate'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Level'
          description: ''
        '400':
          description: Nothing to change; order and track are not editable.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
        '404':
          description: No such object in this organization. A valid id belonging to
            another academy answers the same way — the response never confirms that
            a row exists elsewhere.
  /api/curriculum/organizations/{organization_pk}/placements/:
    post:
      operationId: curriculum_organizations_placements_create
      description: |-
        POST .../placements/ — a student of this academy submits audio or a skip.

        Two gates, both required. ``IsStudent`` is the account role, unchanged from
        Phase 2; ``IsOrganizationMember`` is the tenant, and it is what stops a student
        enrolled at Academy A from submitting into Academy B by knowing its id. The
        track then resolves inside this academy only, and ``PlacementResult.clean()``
        re-checks the membership at the model layer, so the admin and a direct ORM
        write are held to the same rule.

        Re-submitting for a track the student already has a placement for updates that
        row back to pending; it never creates a second one. A minor with no parent link
        yet may still submit — placement is a pre-enrolment step, and
        ``is_fully_active`` gates booking, not this.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlacementSubmit'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PlacementSubmit'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PlacementSubmit'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlacementResult'
          description: Created or updated. A beginner skip comes back already 'reviewed'
            with the track's first level and no reviewer.
        '400':
          description: No such track in this academy, both audio and skip, neither
            of them, or a rejected upload.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not a student account.
  /api/curriculum/organizations/{organization_pk}/placements/{id}/audio-url/:
    get:
      operationId: curriculum_organizations_placements_audio_url_retrieve
      description: |-
        GET .../placements/{id}/audio-url/ — temporary access to a sample.

        Phase 6's authorisation step, with the academy check in front of it. Minting
        the URL *is* the authorisation, so the whole question of who may hear a
        recitation sample is decided here, and SaaS Phase 3 answers it with a
        conjunction:

        .. code-block:: text

            authenticated
                + active member of this academy
                + placement belongs to this academy
                + Phase 6's rule: the lead teacher, or the student themselves

        Nothing was widened. Sub-teachers and parents are still refused — a minor's
        voice recording is the last thing a tenancy phase should open up — and the
        roles are exactly Phase 6's.

        Everything unauthorised is a 404 rather than a 403, because the queryset is
        what narrows it: another academy's placement, another student's placement and a
        beginner skip with no recording all answer the same way, and none of them
        confirms that a row exists.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlacementAudioAccess'
          description: A short-lived URL for the recitation sample. It expires; ask
            again rather than storing it.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member of this academy, or neither the lead teacher
            nor a student.
        '404':
          description: No such placement in this academy, not this student's placement,
            or a beginner skip with no recording.
  /api/curriculum/organizations/{organization_pk}/placements/{id}/review/:
    post:
      operationId: curriculum_organizations_placements_review_create
      description: |-
        POST .../placements/{id}/review/ — this academy's lead sets the level.

        ``Lead A + Placement A`` is allowed and ``Lead A + Placement B`` is a 404, from
        the scoped queryset rather than from a permission class — the placement is not
        in this academy's queryset, so the response cannot confirm it exists. The
        model refuses the same thing again in ``clean()``: a reviewer must be an active
        member of the academy that owns the track.

        Review is still a one-way transition: an already-reviewed placement returns
        409. Correcting a level means the admin, or the student re-submitting.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlacementReview'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PlacementReview'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PlacementReview'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlacementResult'
          description: ''
        '400':
          description: No such level in this academy, or a level from a different
            track than the placement's.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not the lead teacher.
        '404':
          description: No such object in this organization. A valid id belonging to
            another academy answers the same way — the response never confirms that
            a row exists elsewhere.
        '409':
          description: Already reviewed.
  /api/curriculum/organizations/{organization_pk}/placements/children/:
    get:
      operationId: curriculum_organizations_placements_children_list
      description: |-
        GET .../placements/children/ — a parent's linked children, in this academy.

        Three conditions, and the phase spec is explicit that all three must hold: the
        parent is active here, the child is active here, and a ``ParentLink`` exists.
        ``accounts.tenancy.children_in_organization()`` is where that rule lives and
        this view calls it rather than re-deriving it — a second copy is the one that
        would eventually disagree, and the disagreement would be a parent reading
        another academy's student.

        A ``ParentLink`` is a global family fact, not a key: the same parent sees
        different children through Academy A's route and Academy B's.

        Read-only, and no audio. Phase 6 decided that a recitation sample is for the
        lead teacher and the student themselves, and SaaS Phase 3 is forbidden from
        widening that — so a parent can see *that* their child submitted a sample and
        where they were placed, and cannot hear it.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PlacementResult'
          description: Placements of the caller's linked children who are active members
            of this academy. Empty when none are.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not a parent account.
  /api/curriculum/organizations/{organization_pk}/placements/mine/:
    get:
      operationId: curriculum_organizations_placements_mine_list
      description: |-
        GET .../placements/mine/ — the caller's own placements *in this academy*.

        A student studying at two academies calls this twice, once per route, and gets
        two disjoint lists. Their global set of placements is not something any single
        academy is shown.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PlacementResult'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not a student account.
  /api/curriculum/organizations/{organization_pk}/placements/pending/:
    get:
      operationId: curriculum_organizations_placements_pending_list
      description: |-
        GET .../placements/pending/ — this academy's review queue.

        The endpoint Phase 2 got most wrong for a multi-tenant platform: it handed
        every pending placement on the platform to any lead teacher who asked. Now the
        queue is the intersection of "a lead teacher" and "a member of this academy",
        and it contains only placements whose track this academy owns and whose student
        it has admitted.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PlacementResult'
          description: Pending placements in this academy only.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not the lead teacher.
  /api/curriculum/organizations/{organization_pk}/teachers/:
    get:
      operationId: curriculum_organizations_teachers_list
      description: |-
        /api/curriculum/organizations/{id}/teachers/ — who may teach what here.

        Owner and admin only, for reading as well as writing — the one curriculum
        endpoint that is not readable by every member. It follows ``accounts``'
        teacher-configurations endpoint rather than the track and level endpoints
        above, because the list names people and what the academy has entrusted them
        with, which is closer to the membership directory than to a syllabus. The
        teacher themselves reads it from ``teachers/mine/``, so nothing here is hidden
        from the person it is about.

        The membership is resolved from a ``user`` id *inside this academy*, so an
        administrator of one academy cannot touch the same teacher's eligibility in
        another — the phase spec's cross-academy teacher requirement, enforced by the
        lookup rather than by a check that could be forgotten.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TeacherTrack'
          description: This academy's teacher-track assignments.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
    post:
      operationId: curriculum_organizations_teachers_create
      description: |-
        /api/curriculum/organizations/{id}/teachers/ — who may teach what here.

        Owner and admin only, for reading as well as writing — the one curriculum
        endpoint that is not readable by every member. It follows ``accounts``'
        teacher-configurations endpoint rather than the track and level endpoints
        above, because the list names people and what the academy has entrusted them
        with, which is closer to the membership directory than to a syllabus. The
        teacher themselves reads it from ``teachers/mine/``, so nothing here is hidden
        from the person it is about.

        The membership is resolved from a ``user`` id *inside this academy*, so an
        administrator of one academy cannot touch the same teacher's eligibility in
        another — the phase spec's cross-academy teacher requirement, enforced by the
        lookup rather than by a check that could be forgotten.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeacherTrackCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TeacherTrackCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TeacherTrackCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherTrack'
          description: ''
        '400':
          description: Not a member of this academy, an account that cannot teach,
            a track owned elsewhere, or an assignment that already exists.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
  /api/curriculum/organizations/{organization_pk}/teachers/{id}/:
    get:
      operationId: curriculum_organizations_teachers_retrieve
      description: |-
        GET/PATCH one assignment — withdraw or restore eligibility.

        An assignment id from another academy is a 404 from the scoped queryset, which
        is what stops an administrator here from deactivating a teacher's eligibility
        there. PUT is not offered: ``membership`` and ``track`` are what the row *is*.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherTrack'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
        '404':
          description: No such object in this organization. A valid id belonging to
            another academy answers the same way — the response never confirms that
            a row exists elsewhere.
    patch:
      operationId: curriculum_organizations_teachers_partial_update
      description: |-
        GET/PATCH one assignment — withdraw or restore eligibility.

        An assignment id from another academy is a 404 from the scoped queryset, which
        is what stops an administrator here from deactivating a teacher's eligibility
        there. PUT is not offered: ``membership`` and ``track`` are what the row *is*.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTeacherTrackUpdate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTeacherTrackUpdate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTeacherTrackUpdate'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherTrack'
          description: ''
        '400':
          description: Send 'active'.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
        '404':
          description: No such object in this organization. A valid id belonging to
            another academy answers the same way — the response never confirms that
            a row exists elsewhere.
  /api/curriculum/organizations/{organization_pk}/teachers/mine/:
    get:
      operationId: curriculum_organizations_teachers_mine_list
      description: |-
        GET .../teachers/mine/ — what the calling teacher may teach *here*.

        The endpoint that makes the phase visible to a teacher who works for two
        academies. The same account calling it through Academy A's route and Academy
        B's route gets two different lists from the same global identity, because the
        queryset is keyed on the membership the URL resolved to — not on the user.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TeacherTrack'
          description: The caller's own track assignments in this academy, and no
            other academy's.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not a teaching account.
  /api/curriculum/organizations/{organization_pk}/tracks/:
    get:
      operationId: curriculum_organizations_tracks_list
      description: |-
        /api/curriculum/organizations/{id}/tracks/ — the academy's own subjects.

        GET is open to every active member: a student needs the track list to submit a
        placement and a teacher needs it to know what the academy teaches. POST is the
        owner's or an administrator's, because adding a subject is an act of running
        the business (the phase spec leaves teacher authoring to policy; see
        ``permissions.CURRICULUM_MANAGER_ROLES``).

        The created track belongs to the academy in the *route*, whatever the body
        says — there is no ``organization`` field on the write shape at all.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Track'
          description: This academy's tracks, each with its levels in order. Never
            another academy's.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member of this organization.
    post:
      operationId: curriculum_organizations_tracks_create
      description: |-
        /api/curriculum/organizations/{id}/tracks/ — the academy's own subjects.

        GET is open to every active member: a student needs the track list to submit a
        placement and a teacher needs it to know what the academy teaches. POST is the
        owner's or an administrator's, because adding a subject is an act of running
        the business (the phase spec leaves teacher authoring to policy; see
        ``permissions.CURRICULUM_MANAGER_ROLES``).

        The created track belongs to the academy in the *route*, whatever the body
        says — there is no ``organization`` field on the write shape at all.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrackWrite'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/TrackWrite'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TrackWrite'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Track'
          description: ''
        '400':
          description: A slug this academy already uses, or a missing field.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
  /api/curriculum/organizations/{organization_pk}/tracks/{id}/:
    get:
      operationId: curriculum_organizations_tracks_retrieve
      description: |-
        GET/PATCH one of this academy's tracks.

        PATCH renames it or changes its slug. It cannot move the track to another
        academy: ``organization`` is not on the write shape, and ``Track.clean()``
        refuses the change even from the admin or a direct ORM write, because moving a
        track carries its levels, placements and teacher assignments into an academy
        that created none of them.

        PUT is not offered — a whole-object replace would have to accept
        ``organization``.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Track'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member of this organization.
        '404':
          description: No such object in this organization. A valid id belonging to
            another academy answers the same way — the response never confirms that
            a row exists elsewhere.
    patch:
      operationId: curriculum_organizations_tracks_partial_update
      description: |-
        GET/PATCH one of this academy's tracks.

        PATCH renames it or changes its slug. It cannot move the track to another
        academy: ``organization`` is not on the write shape, and ``Track.clean()``
        refuses the change even from the admin or a direct ORM write, because moving a
        track carries its levels, placements and teacher assignments into an academy
        that created none of them.

        PUT is not offered — a whole-object replace would have to accept
        ``organization``.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - curriculum
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedTrackWrite'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedTrackWrite'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedTrackWrite'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Track'
          description: ''
        '400':
          description: Nothing to change, or a taken slug.
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member, or not this academy's owner or administrator.
        '404':
          description: No such object in this organization. A valid id belonging to
            another academy answers the same way — the response never confirms that
            a row exists elsewhere.
  /api/curriculum/placements/{id}/audio/:
    get:
      operationId: curriculum_placements_audio_retrieve
      description: |-
        GET /api/curriculum/placements/{id}/audio/?token=... — the local path.

        Only used when the storage backend cannot sign its own URLs, i.e. local
        private storage in development and tests. Against a private bucket the
        minted URL is presigned and points at the bucket, so nothing reaches here.

        **Deliberately not organization-scoped, and that is not a hole.** The token is
        a bearer capability standing in for a presigned URL, minted only after
        ``AcademyPlacementAudioURLView`` has checked the caller's membership, the
        placement's academy and the Phase 6 role rule. Requiring a tenant in the path
        here would add a check that proves nothing — the holder of the token is
        authorised by having been given it — while an ``<audio src>`` element cannot
        send an Authorization header, which is exactly why presigned URLs exist.

        What it does check, on every request:

        * the token's signature and its age (``read_audio_token``);
        * that the token names *this* placement — a token for one sample cannot be
          pointed at another by editing the path;
        * that the placement still holds the object the token was minted for, so a
          re-submitted sample invalidates outstanding tokens immediately rather than
          at expiry.

        Every failure is the same 404. Telling the caller which check failed would
        let someone probing tokens narrow down why.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: query
        name: token
        schema:
          type: string
        description: The signed token from the audio-url endpoint.
        required: true
      tags:
      - curriculum
      security:
      - {}
      responses:
        '200':
          description: The audio file itself.
        '404':
          description: Missing, invalid, expired or superseded token.
  /api/imports/organizations/{organization_pk}/{id}/commit/:
    post:
      operationId: imports_commit
      description: |-
        Resolves the URL's organization to the caller's own membership, once.

        The permission classes and the querysets both need the same answer to "is this
        caller inside this tenant, and as what", and asking twice is how the two
        eventually disagree. So it is resolved here, cached for the request, and read
        from ``view.caller_membership`` by everything else.

        ``None`` means no access — non-member, suspended member and anonymous caller
        alike — and the permission layer turns that into a refusal before any handler
        runs. Which is why ``organization`` may assume it is not None: by the time a
        handler executes, the caller has an active membership in this organization.
      summary: Commit a validated import job
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - imports
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportJobResponse'
          description: ''
  /api/imports/organizations/{organization_pk}/validate/:
    post:
      operationId: imports_organizations_validate_create
      description: |-
        Resolves the URL's organization to the caller's own membership, once.

        The permission classes and the querysets both need the same answer to "is this
        caller inside this tenant, and as what", and asking twice is how the two
        eventually disagree. So it is resolved here, cached for the request, and read
        from ``view.caller_membership`` by everything else.

        ``None`` means no access — non-member, suspended member and anonymous caller
        alike — and the permission layer turns that into a refusal before any handler
        runs. Which is why ``organization`` may assume it is not None: by the time a
        handler executes, the caller has an active membership in this organization.
      summary: Upload and validate an import file
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - imports
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ImportJobValidate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ImportJobValidate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportJobResponse'
          description: ''
  /api/notifications/organizations/{organization_pk}/{id}/:
    get:
      operationId: notifications_organizations_retrieve
      description: |-
        GET /api/notifications/organizations/<organization_pk>/<id>/ — notification detail.

        Owner/admin can read any notification in this academy.
        Regular members can only read their own notifications in this academy.
        Notifications in other academies or belonging to other users return 404.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - notifications
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
          description: ''
  /api/notifications/organizations/{organization_pk}/{id}/read/:
    post:
      operationId: notifications_organizations_read_create
      description: |-
        POST /api/notifications/organizations/<organization_pk>/<id>/read/ — mark a notification as read.

        A recipient can only mark their own notification as read.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - notifications
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
          description: ''
        '404':
          description: Notification not found for this recipient in this academy.
  /api/notifications/organizations/{organization_pk}/admin/:
    get:
      operationId: notifications_organizations_admin_list
      description: GET /api/notifications/organizations/<organization_pk>/admin/ —
        academy-wide notification history (admin only).
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - notifications
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Notification'
          description: ''
  /api/notifications/organizations/{organization_pk}/deliveries/:
    get:
      operationId: notifications_organizations_deliveries_list
      description: GET /api/notifications/organizations/<organization_pk>/deliveries/
        — academy delivery logs (admin only).
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - notifications
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/NotificationDelivery'
          description: ''
  /api/notifications/organizations/{organization_pk}/mine/:
    get:
      operationId: notifications_organizations_mine_list
      description: GET /api/notifications/organizations/<organization_pk>/mine/ —
        user's own notifications in this academy.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - notifications
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Notification'
          description: ''
  /api/organizations/:
    post:
      operationId: organizations_create
      description: |-
        POST /api/organizations/ — any authenticated user may found an academy.

        The creator becomes the organization's single ``owner`` in the same
        transaction, which the serializer guarantees; there is no request in which one
        happens without the other. Their ``accounts`` role is left exactly as it was:
        a student who founds an academy owns it and is still a student.

        No permission class beyond authentication. Who may create a tenant is a
        signup/billing question, and Phase 1 has neither — restricting it now would be
        inventing a product rule the spec explicitly leaves to later phases.
      tags:
      - organizations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Organization'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Organization'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Organization'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
          description: Created, with the caller as its active owner. Read the membership
            back from /api/organizations/mine/.
        '400':
          description: Taken slug, bad timezone, or no name.
        '401':
          description: Not authenticated.
  /api/organizations/{organization_pk}/audit-logs/:
    get:
      operationId: organizations_audit_logs_list
      description: |-
        GET /api/organizations/{organization_pk}/audit-logs/
        List audit logs for an organization.
      parameters:
      - in: query
        name: action
        schema:
          type: string
      - in: query
        name: actor
        schema:
          type: integer
      - in: query
        name: created_after
        schema:
          type: string
        description: ISO 8601 datetime
      - in: query
        name: created_before
        schema:
          type: string
        description: ISO 8601 datetime
      - in: query
        name: object_id
        schema:
          type: string
      - in: query
        name: object_type
        schema:
          type: string
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAuditLogList'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
  /api/organizations/{organization_pk}/audit-logs/{id}/:
    get:
      operationId: organizations_audit_logs_retrieve
      description: |-
        GET /api/organizations/{organization_pk}/audit-logs/{id}/
        Retrieve a specific audit log for an organization.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
        '404':
          description: Not found or cross-tenant access attempt.
  /api/organizations/{organization_pk}/invitations/:
    get:
      operationId: organizations_invitations_list
      description: /api/organizations/{id}/invitations/
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrganizationInvitation'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
    post:
      operationId: organizations_invitations_create
      description: /api/organizations/{id}/invitations/
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationInvitationCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/OrganizationInvitationCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/OrganizationInvitationCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationInvitation'
          description: ''
        '400':
          description: Invalid invitation data.
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
  /api/organizations/{organization_pk}/invitations/{id}/resend/:
    post:
      operationId: organizations_invitations_resend_create
      description: |-
        POST /api/organizations/{organization_pk}/invitations/{pk}/resend/
        Resends an invitation with a fresh token and resets expiry.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationInvitation'
          description: ''
        '400':
          description: Invitation cannot be resent (accepted or revoked).
        '404':
          description: Invitation not found in this organization.
  /api/organizations/{organization_pk}/invitations/{id}/revoke/:
    post:
      operationId: organizations_invitations_revoke_create
      description: |-
        POST /api/organizations/{organization_pk}/invitations/{pk}/revoke/
        Explicitly revokes a pending invitation.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationInvitation'
          description: ''
        '400':
          description: Only pending invitations can be revoked.
        '404':
          description: Invitation not found in this organization.
  /api/organizations/{organization_pk}/invitations/accept/:
    post:
      operationId: organizations_invitations_accept_create
      description: POST /api/organizations/{id}/invitations/accept/
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationInvitationAccept'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/OrganizationInvitationAccept'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/OrganizationInvitationAccept'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationMembership'
          description: ''
  /api/organizations/{organization_pk}/invitations/preview/:
    get:
      operationId: organizations_invitations_preview_retrieve
      description: |-
        GET /api/organizations/{organization_pk}/invitations/preview/?token=<token>
        Safe public preview of an invitation before authentication/acceptance.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationInvitationPreview'
          description: ''
        '400':
          description: Missing token query parameter.
        '404':
          description: Invitation not found.
  /api/organizations/{organization_pk}/memberships/:
    get:
      operationId: organizations_memberships_list
      description: |-
        /api/organizations/{id}/memberships/ — the tenant's directory, owner/admin only.

        GET lists the organization's memberships. Staff and teacher members are refused
        this: an ordinary member does not receive the academy's member directory in
        Phase 1.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrganizationMembership'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
  /api/organizations/{organization_pk}/memberships/{id}/:
    patch:
      operationId: organizations_memberships_partial_update
      description: |-
        PATCH /api/organizations/{id}/memberships/{member_id}/ — suspend, reactivate, re-role.

        The three membership-management operations the phase spec gives owner and
        admin, behind one partial update: ``status`` suspends or reactivates,
        ``role`` moves a member between ``admin``, ``staff`` and ``teacher``. The row
        is never deleted — a suspended membership is a record that someone was here.

        Two things this endpoint refuses. The owner's membership, to anyone including
        the owner, because demoting or suspending it is an ownership transfer rather
        than a role edit. And a membership belonging to another organization, which is
        a 404 from the queryset: the URL's tenant is the only one whose rows are
        visible here.

        PUT is not offered. A whole-object replace would have to accept
        ``organization`` and ``user``, and neither is editable — a membership pointing
        at a different tenant or a different person is a different membership.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedOrganizationMembershipUpdate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedOrganizationMembershipUpdate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedOrganizationMembershipUpdate'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationMembership'
          description: ''
        '400':
          description: Nothing to change, or a role that is not assignable.
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here, or the target is the organization's
            owner.
        '404':
          description: No such membership in this organization.
  /api/organizations/{organization_pk}/students/:
    get:
      operationId: organizations_students_list
      description: |-
        /api/organizations/<organization_pk>/students/ — the tenant's students.

        GET lists the organization's students; POST attaches an existing student.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/StudentList'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
    post:
      operationId: organizations_students_create
      description: |-
        /api/organizations/<organization_pk>/students/ — the tenant's students.

        GET lists the organization's students; POST attaches an existing student.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StudentEnrollmentCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/StudentEnrollmentCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/StudentEnrollmentCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentList'
          description: ''
        '400':
          description: Unknown user, not a student, or already enrolled
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
  /api/organizations/{organization_pk}/students/{id}/:
    get:
      operationId: organizations_students_retrieve
      description: |-
        /api/organizations/<organization_pk>/students/<id>/ — one student enrollment.

        GET retrieves the student's enrollment record.
        PATCH updates the enrollment status.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentDetail'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
        '404':
          description: No such student in this organization.
    patch:
      operationId: organizations_students_partial_update
      description: |-
        /api/organizations/<organization_pk>/students/<id>/ — one student enrollment.

        GET retrieves the student's enrollment record.
        PATCH updates the enrollment status.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - organizations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedStudentEnrollmentUpdate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedStudentEnrollmentUpdate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedStudentEnrollmentUpdate'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudentDetail'
          description: ''
        '400':
          description: Nothing to change or invalid status
        '401':
          description: Not authenticated.
        '403':
          description: Not an owner or administrator here.
        '404':
          description: No such student in this organization.
  /api/organizations/{id}/:
    get:
      operationId: organizations_retrieve
      description: |-
        GET /api/organizations/{id}/ — one academy, for its members only.

        Knowing the id is not access. A non-member and a suspended member both get
        403, and the object is fetched through the caller's own membership rather than
        from ``Organization.objects``, so there is no queryset here that could return a
        tenant the caller does not belong to.

        Every active role may read the academy — owner, admin, staff and teacher. It is
        the organization's own name, slug and timezone; the things worth protecting
        inside a tenant are its members and, in later phases, its students, schedules
        and money.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
          description: ''
        '401':
          description: Not authenticated.
        '403':
          description: Not an active member of this organization.
  /api/organizations/mine/:
    get:
      operationId: organizations_mine_list
      description: |-
        GET /api/organizations/mine/ — the academies the caller belongs to.

        One entry per active membership, each carrying the organization, the caller's
        role in it and the membership's status — enough for a client to know where the
        user can act and as what. A user in two academies sees two entries.

        Suspended memberships are absent, which is the same rule every other endpoint
        here applies: an active membership is what grants tenant access, and a
        suspended member reading the academy's name and timezone through this listing
        would be the one hole in it. Telling a suspended member *why* they lost access
        is a product decision for the onboarding phase (see learnings.md).
      tags:
      - organizations
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MyOrganizationMembership'
          description: ''
        '401':
          description: Not authenticated.
  /api/payouts/organizations/{organization_pk}/{id}/finalize/:
    post:
      operationId: payouts_organizations_finalize_create
      description: |-
        POST /api/payouts/organizations/<organization_pk>/<int:pk>/finalize/ — make one payout history.

        Owner/admin-only, and one-way: after this the record refuses every write, including
        the lead's own. A payout that is already finalized answers 409 rather than
        silently succeeding, because a second finalization means the caller is acting
        on a stale copy of a financial record.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - payouts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeacherPayout'
          description: Finalized. The record is now immutable.
        '403':
          description: Not an organization owner or administrator.
        '404':
          description: No such payout.
        '409':
          description: Already finalized.
  /api/payouts/organizations/{organization_pk}/generate/:
    post:
      operationId: payouts_organizations_generate_create
      description: |-
        POST /api/payouts/organizations/<organization_pk>/generate/ — turn a period's completed sessions into payouts.

        Safe to repeat, which is the property that makes it usable: a lead who is not
        sure whether the run went through can simply run it again, and the second run
        creates nothing. Existing records are never recalculated, so a rate raised
        yesterday does not reprice last month's payroll.

        The response reports both halves of the run. ``skipped`` is not noise: a
        sub-teacher's session skipped for ``no_payout_rate`` is the lead's cue to set
        a rate, and a group-class seat skipped for ``cohort_session_already_paid``
        explains why a busy session produced one record.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - payouts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PayoutGenerate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PayoutGenerate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PayoutGenerate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenerationResult'
          description: Records created, plus every booking that was skipped and why.
            A repeat run answers 201 with created_count 0.
        '400':
          description: Bad period, or an unknown teacher.
        '403':
          description: Not an organization owner or administrator.
  /api/payouts/organizations/{organization_pk}/lead/:
    get:
      operationId: payouts_organizations_lead_list
      description: |-
        GET /api/payouts/organizations/<organization_pk>/lead/ — academy-wide payout records.

        Filterable by ``?teacher_id=`` and by period. Unfiltered it is every payout
        record in the academy, which is the owner/admin's to see and nobody else's:
        the permission class is the only thing standing between this queryset and a
        sub-teacher reading their colleagues' income, which is why it is the *first*
        thing the class declares.
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Exclusive period end, ISO-8601 UTC.
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: start
        schema:
          type: string
        description: Inclusive period start, ISO-8601 UTC, e.g. 2026-08-01T00:00:00Z.
      - in: query
        name: teacher_id
        schema:
          type: integer
        description: Narrow the listing to one teacher.
      tags:
      - payouts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TeacherPayout'
          description: ''
        '400':
          description: Unknown teacher, or a bad period.
        '403':
          description: Not an organization owner or administrator.
  /api/payouts/organizations/{organization_pk}/mine/:
    get:
      operationId: payouts_organizations_mine_list
      description: |-
        GET /api/payouts/organizations/<organization_pk>/mine/ — the caller's own payout records.

        Optionally bounded by ``?start=&end=``; unbounded it is the teacher's whole
        payout history in this academy, which is theirs to read. The queryset is scoped
        to ``request.user`` and ``self.organization``, and there is no parameter that
        could widen it — another teacher's records are not a 403 here, they simply are
        not in the set.

        Generated and finalized records both appear. A teacher seeing only finalized
        ones would have no way to check a draft before it becomes history.
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Exclusive period end, ISO-8601 UTC.
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: start
        schema:
          type: string
        description: Inclusive period start, ISO-8601 UTC, e.g. 2026-08-01T00:00:00Z.
      tags:
      - payouts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MyTeacherPayout'
          description: ''
        '400':
          description: Unparseable or inverted period.
        '403':
          description: Not an active teacher account in this academy.
  /api/payouts/organizations/{organization_pk}/statements/:
    get:
      operationId: payouts_organizations_statements_retrieve
      description: |-
        GET /api/payouts/organizations/<organization_pk>/statements/?teacher_id=&start=&end= — any teacher's statement.

        The same computation ``statements/mine/`` performs, for a teacher the owner/admin
        names. ``teacher_id`` is required, because a statement is about one teacher —
        an academy-wide total is a different document and Phase 8 was not asked for
        one.
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Exclusive period end, ISO-8601 UTC.
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: start
        schema:
          type: string
        description: Inclusive period start, ISO-8601 UTC, e.g. 2026-08-01T00:00:00Z.
      - in: query
        name: teacher_id
        schema:
          type: integer
        description: Whose statement to compute.
        required: true
      tags:
      - payouts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Statement'
          description: ''
        '400':
          description: Missing teacher_id, unknown teacher, or a bad period.
        '403':
          description: Not an organization owner or administrator.
  /api/payouts/organizations/{organization_pk}/statements/mine/:
    get:
      operationId: payouts_organizations_statements_mine_retrieve
      description: |-
        GET /api/payouts/organizations/<organization_pk>/statements/mine/?start=&end= — the teacher's own statement.

        A statement is computed from the payout records it lists, so the total is
        always the sum of the rows shown underneath it. Both bounds are required: a
        statement is a document about a period.
      parameters:
      - in: query
        name: end
        schema:
          type: string
        description: Exclusive period end, ISO-8601 UTC.
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: start
        schema:
          type: string
        description: Inclusive period start, ISO-8601 UTC, e.g. 2026-08-01T00:00:00Z.
      tags:
      - payouts
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyStatement'
          description: ''
        '400':
          description: Missing, unparseable or inverted period.
        '403':
          description: Not an active teacher account in this academy.
  /api/pricing/organizations/{organization_pk}/agreements/:
    get:
      operationId: pricing_organizations_agreements_list
      description: |-
        /api/pricing/organizations/<organization_pk>/agreements/ — the lead records rates, and reads them back.

        Both halves of the spec's surface on one path, because they are one resource:

        * **POST** records a pricing exception. Lead-only, with no sub-teacher path at
          all: this is the one lever that moves the lead's own margin (mvp-spec
          section 3). Creating an agreement for a student and level that already has a
          live one supersedes the old row rather than editing or deleting it, so the
          history survives.
        * **GET ?student_id=** lists one student's history in this academy — active
          *and* superseded, newest first.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        description: The academy this agreement belongs to.
        required: true
      - in: query
        name: student_id
        schema:
          type: integer
        description: Whose pricing history to list.
        required: true
      tags:
      - pricing
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PricingAgreement'
          description: ''
        '400':
          description: student_id missing or not an integer.
        '403':
          description: Not an active member or not the lead teacher.
    post:
      operationId: pricing_organizations_agreements_create
      description: |-
        /api/pricing/organizations/<organization_pk>/agreements/ — the lead records rates, and reads them back.

        Both halves of the spec's surface on one path, because they are one resource:

        * **POST** records a pricing exception. Lead-only, with no sub-teacher path at
          all: this is the one lever that moves the lead's own margin (mvp-spec
          section 3). Creating an agreement for a student and level that already has a
          live one supersedes the old row rather than editing or deleting it, so the
          history survives.
        * **GET ?student_id=** lists one student's history in this academy — active
          *and* superseded, newest first.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        description: The academy this agreement belongs to.
        required: true
      tags:
      - pricing
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PricingAgreementCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PricingAgreementCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PricingAgreementCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PricingAgreement'
          description: Created and active. Any previous agreement for this student
            and level is now active=False, still queryable.
        '400':
          description: Not a student, an unknown level, or a negative rate.
        '403':
          description: Not an active member or not the lead teacher.
  /api/pricing/organizations/{organization_pk}/agreements/mine/:
    get:
      operationId: pricing_organizations_agreements_mine_list
      description: |-
        GET /api/pricing/organizations/<organization_pk>/agreements/mine/ — the student's own live rates.

        One row per level at most, because only active agreements are listed: a family
        reads what they pay now, not the negotiation that got them there. ``notes`` is
        absent from this shape entirely (see serializers.py).
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        description: The academy to read agreements for.
        required: true
      tags:
      - pricing
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MyPricingAgreement'
          description: ''
        '403':
          description: Not an active member or not a student.
  /api/scheduling/organizations/{organization_pk}/availability/:
    get:
      operationId: scheduling_organizations_availability_list
      description: GET /api/scheduling/organizations/{id}/availability/?teacher_id=
        — one teacher's hours.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: teacher_id
        schema:
          type: integer
        description: Whose availability to list.
        required: true
      tags:
      - scheduling
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Availability'
          description: ''
  /api/scheduling/organizations/{organization_pk}/bookings/:
    post:
      operationId: scheduling_organizations_bookings_create
      description: |-
        POST /api/scheduling/organizations/{id}/bookings/ — a student, or their parent, books a slot.

        Validation of the slot itself lives in ``Booking.clean()``; a rejection
        arrives here as a 400 carrying the model's own message.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BookingCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/BookingCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/BookingCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
          description: Created, with a generated video room and the Jitsi join URL
            both sides use.
        '400':
          description: Outside the teacher's declared hours, overlapping another scheduled
            session, an unapproved teacher, or a minor with no linked parent.
        '403':
          description: Not an active member of this organization.
  /api/scheduling/organizations/{organization_pk}/bookings/{id}/cancel/:
    post:
      operationId: scheduling_organizations_bookings_cancel_create
      description: |-
        POST /api/scheduling/organizations/{id}/bookings/{id}/cancel/ — either party cancels.

        The row is kept and its status set; nothing is deleted, because attendance
        and payouts need the history later.

        The queryset is scoped to bookings the caller is a party to — the student,
        the teacher, or a parent linked to the student (a parent may book, so a
        parent may cancel) — strictly within the route academy. Anyone else gets a 404
        rather than a 403, so this endpoint cannot be used to discover that a
        booking exists.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
          description: ''
        '403':
          description: Not an active member of this organization.
        '404':
          description: No such booking for this caller in this organization.
        '409':
          description: Already cancelled, or completed and now history.
  /api/scheduling/organizations/{organization_pk}/bookings/mine/:
    get:
      operationId: scheduling_organizations_bookings_mine_list
      description: |-
        GET /api/scheduling/organizations/{id}/bookings/mine/ — the student's own sessions.

        Past and upcoming, every status, earliest first. Cancelled ones stay
        visible: they are history, not noise.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Booking'
          description: ''
  /api/scheduling/organizations/{organization_pk}/bookings/teaching/:
    get:
      operationId: scheduling_organizations_bookings_teaching_list
      description: GET /api/scheduling/organizations/{id}/bookings/teaching/ — sessions
        this teacher teaches.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Booking'
          description: ''
  /api/scheduling/organizations/{organization_pk}/cohorts/:
    post:
      operationId: scheduling_organizations_cohorts_create
      description: |-
        POST /api/scheduling/organizations/{id}/cohorts/ — the lead opens a group class.

        Lead-only: a cohort commits a teacher's time, so it is not something a
        sub-teacher grants themselves. Whether the level may run as a group at all,
        and whether the teacher teaches its track, are ``Cohort.clean()``'s calls and
        arrive here as 400s.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CohortCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/CohortCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CohortCreate'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cohort'
          description: ''
        '400':
          description: Level is not group-eligible, the teacher is unapproved, or
            the teacher does not teach the level's track.
        '403':
          description: Not an active member of this organization.
  /api/scheduling/organizations/{organization_pk}/cohorts/open/:
    get:
      operationId: scheduling_organizations_cohorts_open_list
      description: |-
        GET /api/scheduling/organizations/{id}/cohorts/open/?level_id= — cohorts with a seat free.

        The same queryset routing's step 1 reads, exposed so a "browse open cohorts"
        view can be built on it later without a second definition of "open" existing.
        Seat counts are published; the roster is not (see ``CohortSerializer``).
      parameters:
      - in: query
        name: level_id
        schema:
          type: integer
        description: Which level's open cohorts to list.
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Cohort'
          description: ''
  /api/scheduling/organizations/{organization_pk}/route/:
    post:
      operationId: scheduling_organizations_route_create
      description: |-
        POST /api/scheduling/organizations/{id}/route/ — the system picks who teaches.

        Cohort first, then the lead teacher if they have capacity, then a matched
        sub-teacher. The algorithm itself is in ``routing.py``; this view only
        translates its two outcomes into HTTP.

        A refusal is a **409**, not a 200 with ``routed: false`` and not a 400. The
        request was valid and was processed — what stands in the way is the state of
        the academy's capacity, which is precisely what 409 means, and it is the code
        Phase 3's cancel endpoint already uses for "valid request, wrong state". The
        body is structured either way, so a frontend can explain the refusal instead
        of showing a bare error, which is what the spec asks for.

        Phase 5's ``preferred_teacher`` reuses that 409 rather than adding an outcome:
        a waitlisted request is still "valid request, wrong state", and the entry it
        created is reported inside the same ``considered`` payload. So a client that
        already renders the refusal keeps working, and one that wants to say "you are
        on Ustadh's list" reads one extra key.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RouteRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RouteRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RouteRequest'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Routed'
          description: 'Assigned. ``routed_reason`` says how: cohort_assigned, lead_available,
            lead_full_routed, or student_choice when a ``preferred_teacher`` was named
            and could take it. A cohort seat''s start time is the cohort''s own, which
            may differ from the requested one.'
        '400':
          description: 'The request itself is wrong: an unknown level, a parent not
            naming a linked child, a slot the assigned teacher lost to a concurrent
            booking, or a ``preferred_teacher`` who cannot teach this at all (unapproved,
            wrong track) — a refusal no amount of waiting fixes, so no waitlist entry
            is created for it.'
        '403':
          description: Not an active member of this organization.
        '409':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoCapacity'
          description: 'No capacity. Carries why each step declined; nobody is booked
            outside their hours or over their cap to avoid this. For a ``preferred_teacher``
            request this is also the waitlisted outcome: ``considered.waitlist`` carries
            the entry, and no other teacher has been assigned.'
  /api/scheduling/organizations/{organization_pk}/waitlist/{id}/promote/:
    post:
      operationId: scheduling_organizations_waitlist_promote_create
      description: |-
        POST /api/scheduling/organizations/{id}/waitlist/{id}/promote/ — the lead grants the request.

        The whole fulfillment mechanism this phase ships. Automatic offering when a
        slot frees up is explicitly out of scope (spec, tech-debt.md): it needs
        background jobs and notification delivery, which is a phase of its own.

        Lead-only, for the same reason opening a cohort is: it commits a teacher's
        time. The booking goes through ``routing.promote_waitlist_entry``, so it takes
        the teacher's lock and re-validates every rule — an entry made last week
        against a teacher who has since filled up is refused with a 400 rather than
        forced through, which is acceptance criterion 8.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WaitlistPromote'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WaitlistPromote'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WaitlistPromote'
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
          description: Booked. The entry keeps its original request and now carries
            ``fulfilled_booking``; it is not deleted.
        '400':
          description: The teacher is no longer eligible for that slot — full, outside
            their hours, or already booked. The entry stays open.
        '403':
          description: Not an active member of this organization.
        '409':
          description: Already fulfilled.
  /api/scheduling/organizations/{organization_pk}/waitlist/for-teacher/:
    get:
      operationId: scheduling_organizations_waitlist_for_teacher_list
      description: |-
        GET /api/scheduling/organizations/{id}/waitlist/for-teacher/?teacher_id= — the queue to work.

        Open entries only, priority desc then longest-waiting, which comes from
        ``TeacherWaitlist.Meta.ordering`` rather than being re-stated here — the
        endpoint and any future automatic offer must agree about who is next.

        Lead-only. A sub-teacher reading who is waiting for *them* is a reasonable
        thing to want and a different decision (it exposes other families' requests
        to a teacher who cannot act on them), so it is deliberately not folded in
        here — see tech-debt.md.
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      - in: query
        name: teacher_id
        schema:
          type: integer
        description: Whose waitlist to list.
        required: true
      tags:
      - scheduling
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WaitlistEntry'
          description: ''
  /api/scheduling/organizations/{organization_pk}/waitlist/mine/:
    get:
      operationId: scheduling_organizations_waitlist_mine_list
      description: |-
        GET /api/scheduling/organizations/{id}/waitlist/mine/ — the family's own entries and status.

        Open and fulfilled both, newest-priority-first per ``Meta.ordering``. A
        fulfilled entry stays visible because it is the record of a request that was
        honoured — "you asked for Ustadh in March and got the 4th of April" is the
        history the phase exists to keep.

        A **parent** reads their linked children's entries here as well as a student
        reading their own (product owner's call, 2026-08-25). ``/route/`` is
        ``IsStudentOrParent``, so a parent is one of the two people who can *create*
        an entry — and a requester who cannot then read their own request back is a
        hole, not a privacy boundary. Scoped through ``ParentLink`` exactly as
        ``BookingCancelView`` scopes cancellation, and for the same reason: a parent
        acts for their own children and nobody else's.

        Deliberately *not* matched by ``/api/pricing/agreements/mine/``, which stays
        student-only. A waitlist entry is scheduling; a negotiated rate is money, and
        who in a family may read one is a separate decision (tech-debt.md).
      parameters:
      - in: path
        name: organization_pk
        schema:
          type: integer
        required: true
      tags:
      - scheduling
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WaitlistEntry'
          description: ''
components:
  schemas:
    ActionEnum:
      enum:
      - auth.login_success
      - auth.login_failed
      - auth.logout
      - auth.password_changed
      - auth.password_reset_requested
      - organization.created
      - organization.settings_updated
      - membership.created
      - membership.role_changed
      - membership.suspended
      - membership.reactivated
      - invitation.created
      - invitation.resent
      - invitation.revoked
      - invitation.accepted
      - invitation.email_failed
      - permission.changed
      - curriculum.track_created
      - curriculum.track_updated
      - curriculum.track_archived
      - curriculum.level_created
      - curriculum.level_updated
      - curriculum.level_archived
      - curriculum.teacher_assignment_changed
      - enrollment.created
      - enrollment.updated
      - enrollment.withdrawn
      - availability.created
      - availability.updated
      - availability.deleted
      - booking.created
      - booking.updated
      - booking.cancelled
      - booking.completed
      - booking.no_show
      - cohort.created
      - cohort.updated
      - cohort.cancelled
      - cohort.assignment_changed
      - assessment.created
      - assessment.updated
      - assessment.finalized
      - assessment.reopened
      - pricing.agreement_created
      - pricing.agreement_updated
      - pricing.agreement_deactivated
      - pricing.rate_changed
      - payout.created
      - payout.finalized
      - payout.reopened
      - notification.provider_updated
      - notification.configuration_updated
      - notification.sent
      - video.provider_updated
      - video.meeting_created
      - video.meeting_updated
      - video.meeting_cancelled
      - bulk_import.started
      - bulk_import.validated
      - bulk_import.completed
      - bulk_import.failed
      type: string
      description: |-
        * `auth.login_success` - Login Success
        * `auth.login_failed` - Login Failed
        * `auth.logout` - Logout
        * `auth.password_changed` - Password Changed
        * `auth.password_reset_requested` - Password Reset Requested
        * `organization.created` - Organization Created
        * `organization.settings_updated` - Organization Settings Updated
        * `membership.created` - Membership Created
        * `membership.role_changed` - Membership Role Changed
        * `membership.suspended` - Membership Suspended
        * `membership.reactivated` - Membership Reactivated
        * `invitation.created` - Invitation Created
        * `invitation.resent` - Invitation Resent
        * `invitation.revoked` - Invitation Revoked
        * `invitation.accepted` - Invitation Accepted
        * `invitation.email_failed` - Invitation Email Failed
        * `permission.changed` - Permission Changed
        * `curriculum.track_created` - Track Created
        * `curriculum.track_updated` - Track Updated
        * `curriculum.track_archived` - Track Archived
        * `curriculum.level_created` - Level Created
        * `curriculum.level_updated` - Level Updated
        * `curriculum.level_archived` - Level Archived
        * `curriculum.teacher_assignment_changed` - Teacher Assignment Changed
        * `enrollment.created` - Enrollment Created
        * `enrollment.updated` - Enrollment Updated
        * `enrollment.withdrawn` - Enrollment Withdrawn
        * `availability.created` - Availability Created
        * `availability.updated` - Availability Updated
        * `availability.deleted` - Availability Deleted
        * `booking.created` - Booking Created
        * `booking.updated` - Booking Updated
        * `booking.cancelled` - Booking Cancelled
        * `booking.completed` - Booking Completed
        * `booking.no_show` - Booking No Show
        * `cohort.created` - Cohort Created
        * `cohort.updated` - Cohort Updated
        * `cohort.cancelled` - Cohort Cancelled
        * `cohort.assignment_changed` - Cohort Assignment Changed
        * `assessment.created` - Assessment Created
        * `assessment.updated` - Assessment Updated
        * `assessment.finalized` - Assessment Finalized
        * `assessment.reopened` - Assessment Reopened
        * `pricing.agreement_created` - Agreement Created
        * `pricing.agreement_updated` - Agreement Updated
        * `pricing.agreement_deactivated` - Agreement Deactivated
        * `pricing.rate_changed` - Rate Changed
        * `payout.created` - Payout Created
        * `payout.finalized` - Payout Finalized
        * `payout.reopened` - Payout Reopened
        * `notification.provider_updated` - Notification Provider Updated
        * `notification.configuration_updated` - Notification Configuration Updated
        * `notification.sent` - Notification Sent
        * `video.provider_updated` - Video Provider Updated
        * `video.meeting_created` - Meeting Created
        * `video.meeting_updated` - Meeting Updated
        * `video.meeting_cancelled` - Meeting Cancelled
        * `bulk_import.started` - Bulk Import Started
        * `bulk_import.validated` - Bulk Import Validated
        * `bulk_import.completed` - Bulk Import Completed
        * `bulk_import.failed` - Bulk Import Failed
    ActorTypeEnum:
      enum:
      - USER
      - SYSTEM
      type: string
      description: |-
        * `USER` - User
        * `SYSTEM` - System
    AssessmentBooking:
      type: object
      description: The session an assessment is about, in the shape every reader needs
        of it.
      properties:
        id:
          type: integer
          readOnly: true
        level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
        start_time_utc:
          type: string
          format: date-time
          readOnly: true
        duration_minutes:
          type: integer
          readOnly: true
        status:
          type: string
          readOnly: true
      required:
      - duration_minutes
      - id
      - level
      - start_time_utc
      - status
    AssessmentCriterion:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        name:
          type: string
          maxLength: 120
        description:
          type: string
        order:
          type: integer
          maximum: 2147483647
          minimum: 1
          description: Position on the sheet, 1 first. Unique within the rubric.
        active:
          type: boolean
          description: False stops *new* assessments scoring it. Existing scores are
            untouched — deactivating is not deleting.
      required:
      - id
      - name
      - order
    AssessmentCriterionWrite:
      type: object
      description: One criterion in a rubric write. ``id`` present means "edit that
        one".
      properties:
        id:
          type: integer
        name:
          type: string
          maxLength: 120
        description:
          type: string
          maxLength: 2000
        order:
          type: integer
          minimum: 1
        active:
          type: boolean
    AssessmentParty:
      type: object
      description: A person on an assessment. No signup_code, no PII sprawl.
      properties:
        id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/RoleEnum'
          readOnly: true
        timezone:
          type: string
          readOnly: true
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
      required:
      - first_name
      - id
      - last_name
      - role
      - timezone
      - username
    AssessmentRubric:
      type: object
      description: |-
        A rubric and its criteria, as the lead reads them.

        ``criteria`` is every criterion including the retired ones, because a lead
        editing configuration needs to see what they switched off. ``active_criteria``
        is the subset a new assessment would have to score, which is the operationally
        interesting list and is worth not making a client derive.
      properties:
        id:
          type: integer
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        track_id:
          type: integer
          readOnly: true
        name:
          type: string
          readOnly: true
        description:
          type: string
          readOnly: true
        active:
          type: boolean
          readOnly: true
          description: 'False once a later rubric for the same track supersedes this
            one. Superseded rubrics are kept: historical assessments reference their
            criteria.'
        criteria:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentCriterion'
          readOnly: true
        active_criteria:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentCriterion'
          readOnly: true
      required:
      - active
      - active_criteria
      - criteria
      - description
      - id
      - name
      - track
      - track_id
    AssessmentRubricCreate:
      type: object
      description: |-
        The lead configures a track's rubric.

        Creating one for a track that already has a live rubric **supersedes** the old
        row rather than editing or deleting it — the ``PricingAgreement`` pattern, and
        for the same reason: assessments already submitted point at the old rubric's
        criteria, so it has to survive. Renaming or retiring individual criteria is the
        *other* operation, and it is a PATCH on the rubric detail.
      properties:
        track:
          type: integer
        name:
          type: string
          maxLength: 80
        description:
          type: string
          maxLength: 2000
        criteria:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentCriterionWrite'
      required:
      - criteria
      - name
      - track
    AssessmentScore:
      type: object
      description: |-
        One criterion's mark. Shared by every read shape.

        ``criterion_name`` is the snapshot, not a live lookup, so an old assessment
        reads the way the teacher submitted it however the rubric has moved since. The
        live criterion id travels alongside it for anyone aggregating.
      properties:
        id:
          type: integer
          readOnly: true
        criterion:
          type: integer
          readOnly: true
        criterion_name:
          type: string
          readOnly: true
          description: Snapshot of the criterion's name at submission time.
        score:
          type: integer
          readOnly: true
          description: 1 needs significant improvement .. 5 excellent.
        comment:
          type: string
          readOnly: true
          description: Optional per-criterion note. Teaching feedback, not internal
            QC.
      required:
      - comment
      - criterion
      - criterion_name
      - id
      - score
    AssessmentScoreWrite:
      type: object
      description: |-
        One submitted mark. The criterion set is checked by the model, not here.

        ``criterion`` is a bare integer rather than a ``PrimaryKeyRelatedField``
        against every criterion in the academy on purpose: whether an id is a *live
        criterion of this booking's rubric* is exactly the question
        ``SessionAssessment.submit`` answers, and a queryset check here would answer a
        weaker version of it first and report the failure in the wrong words.
      properties:
        criterion:
          type: integer
        score:
          type: integer
          maximum: 5
          minimum: 1
        comment:
          type: string
          maxLength: 2000
      required:
      - criterion
      - score
    AssignableOrganizationRoleEnum:
      enum:
      - admin
      - staff
      - teacher
      - parent
      - student
      type: string
      description: |-
        * `admin` - admin
        * `staff` - staff
        * `teacher` - teacher
        * `parent` - parent
        * `student` - student
    AuditLog:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        organization:
          type: integer
          readOnly: true
        actor:
          type: integer
          readOnly: true
          nullable: true
        actor_type:
          allOf:
          - $ref: '#/components/schemas/ActorTypeEnum'
          readOnly: true
        actor_id_snapshot:
          type: string
          readOnly: true
          description: Preserves actor ID if the user is deleted
        action:
          allOf:
          - $ref: '#/components/schemas/ActionEnum'
          readOnly: true
        object_type:
          type: string
          readOnly: true
        object_id:
          type: string
          readOnly: true
        metadata:
          readOnly: true
        request_id:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - action
      - actor
      - actor_id_snapshot
      - actor_type
      - created_at
      - id
      - metadata
      - object_id
      - object_type
      - organization
      - request_id
    Availability:
      type: object
      description: Read representation of one declared window. Public endpoint.
      properties:
        id:
          type: integer
          readOnly: true
        teacher:
          type: integer
          readOnly: true
        teacher_username:
          type: string
          readOnly: true
        weekday:
          allOf:
          - $ref: '#/components/schemas/WeekdayEnum'
          readOnly: true
        weekday_display:
          type: string
          readOnly: true
        start_time_utc:
          type: string
          format: time
          readOnly: true
          description: UTC. The teacher states local time; entry converts it.
        end_time_utc:
          type: string
          format: time
          readOnly: true
          description: UTC, exclusive. Must be after the start.
        local:
          allOf:
          - $ref: '#/components/schemas/LocalWindow'
          readOnly: true
      required:
      - end_time_utc
      - id
      - local
      - start_time_utc
      - teacher
      - teacher_username
      - weekday
      - weekday_display
    Booking:
      type: object
      description: Read representation, shared by create, both listings, cancel and
        routing.
      properties:
        id:
          type: integer
          readOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/BookingParty'
          readOnly: true
        teacher:
          allOf:
          - $ref: '#/components/schemas/BookingParty'
          readOnly: true
        level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        cohort:
          type: integer
          readOnly: true
          nullable: true
          description: Set when this booking is a seat in a group class rather than
            a 1:1 session. Its teacher, level and start must match the cohort's.
        routed_reason:
          allOf:
          - $ref: '#/components/schemas/RoutedReasonEnum'
          readOnly: true
          description: |-
            How this booking's teacher was decided. Defaults to 'student_choice', which is what Phase 3's direct booking is.

            * `lead_available` - Routed to the lead teacher, who had capacity
            * `lead_full_routed` - Lead teacher full, routed to a sub teacher
            * `student_choice` - Teacher named directly by the student or parent
            * `cohort_assigned` - Assigned a seat in an open cohort
        start_time_utc:
          type: string
          format: date-time
          readOnly: true
          description: Stored UTC, always.
        start_time_local:
          type: string
          nullable: true
          description: start_time_utc in the caller's own zone, so nobody does the
            maths.
          readOnly: true
        end_time_utc:
          type: string
          format: date-time
          readOnly: true
        duration_minutes:
          type: integer
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/BookingStatusEnum'
          readOnly: true
        video_provider:
          type: string
          readOnly: true
        video_provider_meeting_id:
          type: string
          readOnly: true
        video_join_url:
          type: string
          readOnly: true
      required:
      - cohort
      - duration_minutes
      - end_time_utc
      - id
      - level
      - routed_reason
      - start_time_local
      - start_time_utc
      - status
      - student
      - teacher
      - track
      - video_join_url
      - video_provider
      - video_provider_meeting_id
    BookingCreate:
      type: object
      description: |-
        A student books themselves, or a parent books a linked child.

        Everything about *whether* the slot is legal — declared hours, overlap, an
        approved teacher, the track specialty, the weekly cap — belongs to
        ``Booking.clean()`` and is surfaced from there, so the API and a direct ORM
        write cannot disagree. This serializer only resolves who the session is for.

        ``routed_reason`` is not a client field: naming your own teacher *is*
        ``student_choice``, which the model already defaults to. Phase 4's other
        three reasons are the routing engine's to record.
      properties:
        teacher:
          type: integer
        level:
          type: integer
        start_time_utc:
          type: string
          format: date-time
        duration_minutes:
          type: integer
          minimum: 1
          default: 30
        student:
          type: integer
          description: Required when a parent books; ignored when a student books.
      required:
      - level
      - start_time_utc
      - teacher
    BookingParty:
      type: object
      description: A person on one side of a booking. No signup_code, no sprawl.
      properties:
        id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/RoleEnum'
          readOnly: true
        timezone:
          type: string
          readOnly: true
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
      required:
      - first_name
      - id
      - last_name
      - role
      - timezone
      - username
    BookingStatusEnum:
      enum:
      - scheduled
      - completed
      - no_show
      - cancelled
      type: string
      description: |-
        * `scheduled` - Scheduled
        * `completed` - Completed
        * `no_show` - No show
        * `cancelled` - Cancelled
    ChannelEnum:
      enum:
      - email
      - whatsapp
      - telegram
      - push
      - in_app
      type: string
      description: |-
        * `email` - Email
        * `whatsapp` - WhatsApp
        * `telegram` - Telegram
        * `push` - Push
        * `in_app` - In-App
    Cohort:
      type: object
      description: |-
        Read representation of a group class.

        Seat *counts* are published; the seated students are not. Who else is in a
        class is not something the open-cohorts endpoint needs to tell every
        authenticated caller, and a roster is a different endpoint's decision to make.
      properties:
        id:
          type: integer
          readOnly: true
        teacher:
          allOf:
          - $ref: '#/components/schemas/BookingParty'
          readOnly: true
        level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        max_students:
          type: integer
          readOnly: true
        seats_taken:
          type: integer
          readOnly: true
        seats_available:
          type: integer
          readOnly: true
        schedule_start_utc:
          type: string
          format: date-time
          readOnly: true
          description: Stored UTC, always.
        schedule_start_local:
          type: string
          nullable: true
          description: The start in the caller's zone, falling back to the teacher's.
          readOnly: true
      required:
      - id
      - level
      - max_students
      - schedule_start_local
      - schedule_start_utc
      - seats_available
      - seats_taken
      - teacher
      - track
    CohortCreate:
      type: object
      description: |-
        The lead teacher opens a group class.

        ``students`` is deliberately not writable: seats are given by routing or by
        ``Cohort.add_student``, both of which create the seat's ``Booking`` too. A
        cohort that could be created with students already in it would have members
        holding no session.
      properties:
        teacher:
          type: integer
        level:
          type: integer
        max_students:
          type: integer
          maximum: 2147483647
          minimum: 1
          default: 6
        schedule_start_utc:
          type: string
          format: date-time
          description: Stored UTC, always.
      required:
      - level
      - schedule_start_utc
      - teacher
    CriterionAverage:
      type: object
      description: One criterion's average over a period. ``average`` is null when
        unscored.
      properties:
        criterion_id:
          type: integer
          readOnly: true
        criterion_name:
          type: string
          readOnly: true
        average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        score_count:
          type: integer
          readOnly: true
      required:
      - average
      - criterion_id
      - criterion_name
      - score_count
    EventTypeEnum:
      enum:
      - booking_confirmed
      - booking_cancelled
      - placement_reviewed
      - progress_ready
      - teacher_invitation
      type: string
      description: |-
        * `booking_confirmed` - Booking Confirmed
        * `booking_cancelled` - Booking Cancelled
        * `placement_reviewed` - Placement Reviewed
        * `progress_ready` - Progress Ready
        * `teacher_invitation` - Teacher Invitation
    FamilyAssessment:
      type: object
      description: |-
        What a student or their linked parent reads.

        Teaching content and nothing else: the summary the teacher wrote for them, the
        criterion scores and comments, who taught it. Every internal quality-control
        field is absent from the shape rather than blanked — no ``flagged_for_review``,
        no ``flag_reason``, no ``lead_*`` at all.
      properties:
        id:
          type: integer
          readOnly: true
        booking:
          allOf:
          - $ref: '#/components/schemas/AssessmentBooking'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        assessed_by:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          readOnly: true
        assessed_at:
          type: string
          format: date-time
          readOnly: true
          description: When it was submitted. Stored UTC, rendered per-viewer.
        assessed_at_local:
          type: string
          nullable: true
          readOnly: true
        rubric_name:
          type: string
          readOnly: true
          description: Snapshot of the rubric's name at submission time.
        scores:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentScore'
          readOnly: true
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
        teacher_summary:
          type: string
          readOnly: true
          description: 'Written for the family: this is the one free-text field they
            read.'
      required:
      - assessed_at
      - assessed_at_local
      - assessed_by
      - booking
      - id
      - overall_average
      - rubric_name
      - scores
      - teacher_summary
      - track
    FamilyProgressSnapshot:
      type: object
      description: |-
        The family's view. No ``generated_by``, and only published rows reach it.

        ``visible_to_family`` is absent from the shape because the endpoint already
        filters on it: a row a family can read is published by definition, so
        publishing it again as a field would be noise. Which rows those are is the
        view's decision, not this class's.
      properties:
        id:
          type: integer
          readOnly: true
        track:
          allOf:
          - $ref: '#/components/schemas/TrackBrief'
          readOnly: true
        period_start:
          type: string
          format: date-time
          readOnly: true
          description: Inclusive. Stored UTC.
        period_end:
          type: string
          format: date-time
          readOnly: true
          description: Exclusive. Stored UTC.
        generated_at:
          type: string
          format: date-time
          readOnly: true
        completed_sessions:
          type: integer
          readOnly: true
          description: Sessions taught in the period, assessed or not.
        assessed_sessions:
          type: integer
          readOnly: true
          description: How many of those carry an assessment. Never inflated to match.
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: 'Null when nothing in the period was assessed. Deliberately
            nullable: ''no assessments'' is missing data, and 0.00 would read as a
            score of zero out of five.'
        criterion_averages:
          readOnly: true
          description: 'Snapshot of the per-criterion averages: [{criterion_id, criterion_name,
            average, score_count}].'
        summary:
          type: string
          readOnly: true
          description: Short human line. Auto-composed from the numbers unless the
            lead supplies one.
      required:
      - assessed_sessions
      - completed_sessions
      - criterion_averages
      - generated_at
      - id
      - overall_average
      - period_end
      - period_start
      - summary
      - track
    GenerationResult:
      type: object
      description: What a generation run did — created records, and skipped bookings.
      properties:
        period_start:
          type: string
          format: date-time
          readOnly: true
        period_end:
          type: string
          format: date-time
          readOnly: true
        created_count:
          type: integer
          readOnly: true
        skipped_count:
          type: integer
          readOnly: true
        total_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          readOnly: true
        created:
          type: array
          items:
            $ref: '#/components/schemas/TeacherPayout'
          readOnly: true
        skipped:
          type: array
          items:
            $ref: '#/components/schemas/SkippedBooking'
          readOnly: true
      required:
      - created
      - created_count
      - period_end
      - period_start
      - skipped
      - skipped_count
      - total_amount
    ImportJobResponse:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        organization:
          type: integer
          readOnly: true
        created_by:
          type: integer
          readOnly: true
        kind:
          allOf:
          - $ref: '#/components/schemas/KindEnum'
          readOnly: true
        file_name:
          type: string
          readOnly: true
        file_size:
          type: integer
          readOnly: true
        file_type:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/ImportJobResponseStatusEnum'
          readOnly: true
        column_mapping:
          readOnly: true
        row_count:
          type: integer
          readOnly: true
        valid_row_count:
          type: integer
          readOnly: true
        invalid_row_count:
          type: integer
          readOnly: true
        created_count:
          type: integer
          readOnly: true
        updated_count:
          type: integer
          readOnly: true
        skipped_count:
          type: integer
          readOnly: true
        error_count:
          type: integer
          readOnly: true
        invitations_created:
          type: integer
          readOnly: true
        emails_sent:
          type: integer
          readOnly: true
        emails_failed:
          type: integer
          readOnly: true
        error_report:
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        started_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        completed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
      required:
      - column_mapping
      - completed_at
      - created_at
      - created_by
      - created_count
      - emails_failed
      - emails_sent
      - error_count
      - error_report
      - file_name
      - file_size
      - file_type
      - id
      - invalid_row_count
      - invitations_created
      - kind
      - organization
      - row_count
      - skipped_count
      - started_at
      - status
      - updated_count
      - valid_row_count
    ImportJobResponseStatusEnum:
      enum:
      - uploaded
      - validated
      - failed
      - running
      - completed
      - partially_completed
      type: string
      description: |-
        * `uploaded` - Uploaded
        * `validated` - Validated
        * `failed` - Failed
        * `running` - Running
        * `completed` - Completed
        * `partially_completed` - Partially Completed
    ImportJobValidate:
      type: object
      properties:
        file:
          type: string
          format: uri
        kind:
          $ref: '#/components/schemas/KindEnum'
        column_mapping: {}
      required:
      - file
      - kind
    InvitableOrganizationRoleEnum:
      enum:
      - admin
      - teacher
      - parent
      - student
      type: string
      description: |-
        * `admin` - admin
        * `teacher` - teacher
        * `parent` - parent
        * `student` - student
    KindEnum:
      enum:
      - teachers
      - students
      - parents
      type: string
      description: |-
        * `teachers` - Teachers
        * `students` - Students
        * `parents` - Parents
    LeadAssessment:
      type: object
      description: 'The lead''s view: everything, including the flag reason and the
        review note.'
      properties:
        id:
          type: integer
          readOnly: true
        booking:
          allOf:
          - $ref: '#/components/schemas/AssessmentBooking'
          readOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/AssessmentParty'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        assessed_by:
          allOf:
          - $ref: '#/components/schemas/AssessmentParty'
          readOnly: true
        assessed_at:
          type: string
          format: date-time
          readOnly: true
          description: When it was submitted. Stored UTC, rendered per-viewer.
        assessed_at_local:
          type: string
          nullable: true
          readOnly: true
        rubric_name:
          type: string
          readOnly: true
          description: Snapshot of the rubric's name at submission time.
        criteria_snapshot:
          readOnly: true
          description: 'Snapshot of the criteria scored: [{id, name, order}]. What
            the teacher was actually looking at, frozen.'
        scores:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentScore'
          readOnly: true
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
        teacher_summary:
          type: string
          readOnly: true
          description: 'Written for the family: this is the one free-text field they
            read.'
        flagged_for_review:
          type: boolean
          readOnly: true
          description: The teacher asking the lead to look at this session.
        flag_reason:
          type: string
          readOnly: true
          description: Why it was flagged. Internal — never shown to a student or
            parent.
        lead_reviewed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Set by an explicit lead action. Viewing an assessment does
            not set it.
        lead_reviewed_by:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          readOnly: true
        lead_review_note:
          type: string
          readOnly: true
          description: The lead's private annotation. Not visible to the family, and
            not visible to the assessing sub-teacher either in this phase.
      required:
      - assessed_at
      - assessed_at_local
      - assessed_by
      - booking
      - criteria_snapshot
      - flag_reason
      - flagged_for_review
      - id
      - lead_review_note
      - lead_reviewed_at
      - lead_reviewed_by
      - overall_average
      - rubric_name
      - scores
      - student
      - teacher_summary
      - track
    LeadReview:
      type: object
      description: |-
        The lead annotates an assessment and marks it reviewed.

        A note and nothing else is writable, which is the whole point: review is an
        annotation beside the teacher's scores, never an edit of them. The model
        refuses the edit too, so this is the polite layer rather than the only one.
      properties:
        lead_review_note:
          type: string
          maxLength: 4000
    Level:
      type: object
      description: |-
        One rung of a track's ladder.

        ``track`` is on the read shape because the academy-scoped level endpoints are
        flat — a client listing an academy's levels needs to know which track each one
        belongs to, and the level's academy is exactly its track's academy.
      properties:
        id:
          type: integer
          readOnly: true
        track:
          type: integer
          readOnly: true
        order:
          type: integer
          readOnly: true
          description: Sequence within the track, 1 = first. Appended, never inserted.
        name:
          type: string
          readOnly: true
        min_age:
          type: integer
          readOnly: true
          nullable: true
          description: Informational only in this phase — not a hard gate. A 10-year-old
            and a 40-year-old can both sit at Beginner Arabic.
        group_eligible:
          type: boolean
          readOnly: true
          description: Whether this level may run as a cohort. Cohorts arrive in Phase
            4; the data is captured now so routing has something to read.
      required:
      - group_eligible
      - id
      - min_age
      - name
      - order
      - track
    LevelCreate:
      type: object
      description: |-
        Appending a level to one of this academy's tracks.

        ``order`` is deliberately not accepted. The Phase 2 rule is that levels are
        appended and never inserted, and a server that computes the next order cannot
        be argued with — there is no request that expresses "insert at 2", so the rule
        holds without having to reject anything. ``Level.next_order_for()`` is the same
        function the model validates against, so the two cannot disagree.

        ``track`` resolves inside this academy only, which is what rejects the phase
        spec's named attack: Academy A's route plus Academy B's track id.
      properties:
        track:
          type: integer
        name:
          type: string
          maxLength: 80
        min_age:
          type: integer
          minimum: 1
          nullable: true
          description: Informational only — not a hard gate on who may sit the level.
        group_eligible:
          type: boolean
          default: false
      required:
      - name
      - track
    LinkedStudent:
      type: object
      description: A child as seen by their linked parent — no signup_code, no PII
        sprawl.
      properties:
        id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
        email:
          type: string
          format: email
          readOnly: true
          title: Email address
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        timezone:
          type: string
          readOnly: true
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
        date_of_birth:
          type: string
          format: date
          readOnly: true
          nullable: true
        is_minor:
          type: boolean
          readOnly: true
          description: Snapshot computed from date_of_birth at signup. Not recomputed
            on birthdays — see learnings.md.
        is_fully_active:
          type: boolean
          readOnly: true
      required:
      - date_of_birth
      - email
      - first_name
      - id
      - is_fully_active
      - is_minor
      - last_name
      - timezone
      - username
    LocalWindow:
      type: object
      description: A weekly window as it reads in one particular timezone.
      properties:
        timezone:
          type: string
        weekday:
          type: integer
          description: May differ from the UTC weekday — a conversion can cross midnight.
        start_time:
          type: string
          format: time
        end_time:
          type: string
          format: time
      required:
      - end_time
      - start_time
      - timezone
      - weekday
    Login:
      type: object
      properties:
        username:
          type: string
        email:
          oneOf:
          - type: string
            format: email
          - type: string
            maxLength: 0
        password:
          type: string
      required:
      - password
    MembershipStatusEnum:
      enum:
      - active
      - suspended
      type: string
      description: |-
        * `active` - Active
        * `suspended` - Suspended
    MyOrganizationMembership:
      type: object
      description: |-
        The caller's own membership, with the academy it is in — the ``/mine/`` shape.

        Membership id, organization, organization role and status: enough for a client
        to know which academies the user is in and what they may do in each, which is
        what the spec asks of this endpoint. Another user's membership never appears
        here; the view scopes the queryset to ``request.user``.
      properties:
        id:
          type: integer
          readOnly: true
        organization:
          allOf:
          - $ref: '#/components/schemas/Organization'
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/OrganizationRoleEnum'
          readOnly: true
          description: |-
            Authority inside this academy. Independent of User.role — a user may be 'teacher' here and 'student' globally.

            * `owner` - Owner
            * `admin` - Administrator
            * `staff` - Staff
            * `teacher` - Teacher
            * `parent` - Parent
            * `student` - Student
        role_display:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/MembershipStatusEnum'
          readOnly: true
          description: |-
            active grants tenant access; suspended keeps the row and revokes it.

            * `active` - Active
            * `suspended` - Suspended
        status_display:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - id
      - organization
      - role
      - role_display
      - status
      - status_display
    MyPricingAgreement:
      type: object
      description: |-
        The family's view: their rate, without the lead's reasoning.

        ``notes`` and ``approved_by`` are both absent. The note is private by
        instruction; the approver is omitted because there is exactly one lead and
        "who agreed this" is internal bookkeeping rather than something the family
        needs from this endpoint.
      properties:
        id:
          type: integer
          readOnly: true
        level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        standard_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          description: What this level would normally cost, snapshotted when the agreement
            was made. Deliberately stored rather than derived, so changing the standard
            rate later cannot silently reprice an existing agreement.
        agreed_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          description: What was actually negotiated. May equal standard_rate.
        reason:
          allOf:
          - $ref: '#/components/schemas/ReasonEnum'
          readOnly: true
        reason_display:
          type: string
          readOnly: true
        active:
          type: boolean
          readOnly: true
          description: False once a later agreement for the same student and level
            supersedes this one. Superseded rows are kept, never deleted.
      required:
      - active
      - agreed_rate
      - id
      - level
      - reason
      - reason_display
      - standard_rate
      - track
    MyStatement:
      type: object
      description: |-
        A teacher's own statement for one period.

        Computed from the payout records listed inside it, so the total and the rows
        cannot disagree — there is no statement table to fall out of step with them.
        ``status`` says how settled the period is; ``empty`` means no payout record
        exists for it yet, which is not the same statement as "you earned nothing".
      properties:
        period_start:
          type: string
          format: date-time
          readOnly: true
        period_end:
          type: string
          format: date-time
          readOnly: true
        session_count:
          type: integer
          readOnly: true
        finalized_count:
          type: integer
          readOnly: true
        total_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          readOnly: true
        currency:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/StatementStatusEnum'
          readOnly: true
        payouts:
          type: array
          items:
            $ref: '#/components/schemas/MyTeacherPayout'
          readOnly: true
      required:
      - currency
      - finalized_count
      - payouts
      - period_end
      - period_start
      - session_count
      - status
      - total_amount
    MyTeacherPayout:
      type: object
      description: |-
        The teacher's own payout: what they earned, and for which session.

        ``teacher`` is absent — the caller is the only possible answer. Everything
        that produced the amount is present, because a payout the teacher cannot
        check is not transparent: ``minutes_paid`` and ``rate_used`` are the two
        inputs, and ``amount`` is what the one formula made of them.
      properties:
        id:
          type: integer
          readOnly: true
        booking:
          allOf:
          - $ref: '#/components/schemas/PayoutSession'
          readOnly: true
        cohort:
          type: integer
          readOnly: true
          nullable: true
          description: Set when the paid session is a group class, so the unique constraint
            below can enforce one payout per cohort session. Always equal to booking.cohort.
        minutes_paid:
          type: integer
          readOnly: true
          description: Session length used in the calculation, snapshotted from the
            booking. Stored rather than derived so the record explains its own amount
            without depending on a row that could later be corrected.
        rate_used:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          description: 'The teacher''s hourly rate at generation time, snapshotted
            the same way PricingAgreement.standard_rate is: changing the live rate
            must not reprice a payout that has already been made.'
        amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,2})?$
          readOnly: true
          description: minutes_paid / 60 × rate_used, rounded once. See payout_amount().
        currency:
          type: string
          readOnly: true
          description: ISO code, snapshotted. NGN today; conversion is out of scope.
        status:
          allOf:
          - $ref: '#/components/schemas/PayoutStatusEnum'
          readOnly: true
          description: |-
            generated → finalized. Finalized records are immutable.

            * `generated` - Generated
            * `finalized` - Finalized
        status_display:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        finalized_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Stamped when the lead finalizes. Null while generated.
      required:
      - amount
      - booking
      - cohort
      - created_at
      - currency
      - finalized_at
      - id
      - minutes_paid
      - rate_used
      - status
      - status_display
    NoCapacity:
      type: object
      description: The honest failure. Documented as a schema so a client can rely
        on it.
      properties:
        routed:
          type: boolean
        reason:
          type: string
        detail:
          type: string
        considered:
          type: object
          additionalProperties: {}
          description: 'Why each step declined, keyed by step: ''cohort'', ''lead'',
            ''sub_teachers''. Present so the frontend can explain the refusal rather
            than showing a bare error. A preferred-teacher request adds ''preferred_teacher''
            (the refusal for that teacher) and ''waitlist'' (the waitlist entry created,
            when one was) — the Phase 4 shape extended, not replaced.'
      required:
      - considered
      - detail
      - reason
      - routed
    Notification:
      type: object
      description: Academy notification serializer with masked deliveries and read
        status.
      properties:
        id:
          type: integer
          readOnly: true
        organization_id:
          type: integer
          description: The academy this notification belongs to.
          readOnly: true
        event_type:
          allOf:
          - $ref: '#/components/schemas/EventTypeEnum'
          readOnly: true
          description: |-
            The event type triggering this notification.

            * `booking_confirmed` - Booking Confirmed
            * `booking_cancelled` - Booking Cancelled
            * `placement_reviewed` - Placement Reviewed
            * `progress_ready` - Progress Ready
            * `teacher_invitation` - Teacher Invitation
        event_type_display:
          type: string
          readOnly: true
        recipient_id:
          type: integer
          description: The recipient of this notification.
          readOnly: true
        recipient_username:
          type: string
          readOnly: true
        title:
          type: string
          readOnly: true
          description: Short headline summary.
        summary:
          type: string
          readOnly: true
          description: Human-readable notification text.
        payload:
          readOnly: true
          description: JSON-serializable domain payload stripped of secrets or provider
            details.
        is_read:
          type: boolean
          readOnly: true
        read_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Timestamp when recipient marked this notification read.
        created_at:
          type: string
          format: date-time
          readOnly: true
        deliveries:
          type: array
          items:
            $ref: '#/components/schemas/NotificationDelivery'
          readOnly: true
      required:
      - created_at
      - deliveries
      - event_type
      - event_type_display
      - id
      - is_read
      - organization_id
      - payload
      - read_at
      - recipient_id
      - recipient_username
      - summary
      - title
    NotificationDelivery:
      type: object
      description: Delivery record serializer — strictly provider secrets/credentials
        free.
      properties:
        id:
          type: integer
          readOnly: true
        notification_id:
          type: integer
          description: The notification being delivered.
          readOnly: true
        channel:
          allOf:
          - $ref: '#/components/schemas/ChannelEnum'
          readOnly: true
          description: |-
            Channel used for delivery.

            * `email` - Email
            * `whatsapp` - WhatsApp
            * `telegram` - Telegram
            * `push` - Push
            * `in_app` - In-App
        channel_display:
          type: string
          readOnly: true
        provider:
          type: string
          readOnly: true
          description: Provider adapter identifier (e.g., 'email_smtp', 'meta_whatsapp').
        status:
          allOf:
          - $ref: '#/components/schemas/NotificationDeliveryStatusEnum'
          readOnly: true
          description: |-
            Delivery status outcome.

            * `pending` - Pending
            * `sent` - Sent
            * `delivered` - Delivered
            * `failed` - Failed
        status_display:
          type: string
          readOnly: true
        attempt_count:
          type: integer
          readOnly: true
          description: Number of delivery attempts executed.
        provider_message_id:
          type: string
          readOnly: true
          description: Identifier returned by the external provider.
        error_code:
          type: string
          readOnly: true
          description: Machine-readable error code if failed.
        error_message:
          type: string
          readOnly: true
          description: Human-readable error details if failed.
        attempted_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Timestamp of the latest delivery attempt.
        delivered_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Timestamp when delivery was confirmed.
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - attempt_count
      - attempted_at
      - channel
      - channel_display
      - created_at
      - delivered_at
      - error_code
      - error_message
      - id
      - notification_id
      - provider
      - provider_message_id
      - status
      - status_display
    NotificationDeliveryStatusEnum:
      enum:
      - pending
      - sent
      - delivered
      - failed
      type: string
      description: |-
        * `pending` - Pending
        * `sent` - Sent
        * `delivered` - Delivered
        * `failed` - Failed
    Organization:
      type: object
      description: |-
        An academy — the read shape, and the one that creates a tenant.

        ``is_active`` is read-only: disabling an academy is a later phase's workflow
        with its own consequences for the people inside it, not a field a creation
        request gets to set. ``timezone`` is validated by the model's own IANA
        validator, which the ``ModelSerializer`` picks up, so the API and a direct ORM
        write reject the same strings.
      properties:
        id:
          type: integer
          readOnly: true
        name:
          type: string
          description: Human-readable institution name, e.g. 'Al-Huda Quran Academy'.
            Not unique — two academies may legitimately choose similar names.
          maxLength: 120
        slug:
          type: string
          description: Machine-readable identifier, e.g. 'al-huda-quran-academy'.
            Globally unique, because the organization is itself the tenant and so
            has nothing to be scoped within.
          maxLength: 64
          pattern: ^[-a-zA-Z0-9_]+$
        timezone:
          type: string
          description: IANA zone name, e.g. 'Africa/Lagos'. The academy's own zone,
            validated by the same rule as User.timezone.
          maxLength: 64
        is_active:
          type: boolean
          readOnly: true
          description: Whether the academy is operating. Disabling an organization
            is a later phase's workflow, so nothing reads this yet — access is decided
            by active membership alone (see learnings.md).
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - id
      - is_active
      - name
      - slug
      - timezone
      - updated_at
    OrganizationInvitation:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        email:
          type: string
          format: email
          readOnly: true
          description: The email address invited. Used to match the user on acceptance.
        role:
          allOf:
          - $ref: '#/components/schemas/OrganizationRoleEnum'
          readOnly: true
          description: |-
            The role the user will hold upon acceptance.

            * `owner` - Owner
            * `admin` - Administrator
            * `staff` - Staff
            * `teacher` - Teacher
            * `parent` - Parent
            * `student` - Student
        status:
          allOf:
          - $ref: '#/components/schemas/OrganizationInvitationStatusEnum'
          readOnly: true
        expires_at:
          type: string
          format: date-time
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        email_delivery_status:
          type: string
          readOnly: true
      required:
      - created_at
      - email
      - email_delivery_status
      - expires_at
      - id
      - role
      - status
    OrganizationInvitationAccept:
      type: object
      description: Accepts an invitation using its token.
      properties:
        token:
          type: string
      required:
      - token
    OrganizationInvitationCreate:
      type: object
      description: Creates a pending invitation for an email address.
      properties:
        email:
          type: string
          format: email
        role:
          $ref: '#/components/schemas/InvitableOrganizationRoleEnum'
      required:
      - email
      - role
    OrganizationInvitationPreview:
      type: object
      description: Safe public representation of an invitation for the acceptance
        screen.
      properties:
        organization_id:
          type: integer
          readOnly: true
        organization_name:
          type: string
          readOnly: true
        role:
          type: string
          readOnly: true
        status:
          type: string
          readOnly: true
        expires_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - expires_at
      - organization_id
      - organization_name
      - role
      - status
    OrganizationInvitationStatusEnum:
      enum:
      - pending
      - accepted
      - expired
      - revoked
      type: string
      description: |-
        * `pending` - Pending
        * `accepted` - Accepted
        * `expired` - Expired
        * `revoked` - Revoked
    OrganizationMembership:
      type: object
      description: |-
        One membership, as an owner or admin reads it in the tenant's directory.

        ``username`` travels with the user id because a membership list is read by a
        human deciding who to suspend or promote, and an id alone does not identify a
        person. No email, no signup code, no account detail beyond the name: this is a
        membership record, not a user directory, and ``accounts`` owns the latter.
      properties:
        id:
          type: integer
          readOnly: true
        organization:
          type: integer
          readOnly: true
        user:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/OrganizationRoleEnum'
          readOnly: true
          description: |-
            Authority inside this academy. Independent of User.role — a user may be 'teacher' here and 'student' globally.

            * `owner` - Owner
            * `admin` - Administrator
            * `staff` - Staff
            * `teacher` - Teacher
            * `parent` - Parent
            * `student` - Student
        role_display:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/MembershipStatusEnum'
          readOnly: true
          description: |-
            active grants tenant access; suspended keeps the row and revokes it.

            * `active` - Active
            * `suspended` - Suspended
        status_display:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - id
      - organization
      - role
      - role_display
      - status
      - status_display
      - updated_at
      - user
      - username
    OrganizationRoleEnum:
      enum:
      - owner
      - admin
      - staff
      - teacher
      - parent
      - student
      type: string
      description: |-
        * `owner` - Owner
        * `admin` - Administrator
        * `staff` - Staff
        * `teacher` - Teacher
        * `parent` - Parent
        * `student` - Student
    OrganizationTeacherConfiguration:
      type: object
      description: |-
        One academy's terms for one teacher, as an owner or admin reads them.

        ``user`` and ``organization`` travel as ids alongside the membership because a
        client reading a list of teaching terms needs to know whose they are without a
        second request, and ``username`` because an id does not identify a person to the
        human deciding whether to approve them. Nothing else from the account: this is a
        teaching-terms record, not a user directory.

        Entirely read-only. Writes go through the create and update shapes below, so
        there is no path here that could move a configuration to another membership.
      properties:
        id:
          type: integer
          readOnly: true
        membership:
          type: integer
          readOnly: true
          description: The academy-and-teacher relationship these terms apply to.
        user:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
        organization:
          type: integer
          readOnly: true
        max_weekly_hours:
          type: integer
          readOnly: true
          description: Capacity dial for this academy only. Scheduling still enforces
            TeacherProfile.max_weekly_hours until scheduling tenancy lands.
        hourly_payout_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: What this academy pays per hour. Null means no per-hour rate,
            as on TeacherProfile. Payout calculation still reads that one.
        approved:
          type: boolean
          readOnly: true
          description: Approved to teach *here*. An academy approves its own teachers,
            so a teacher may be approved by one and not another.
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - approved
      - created_at
      - hourly_payout_rate
      - id
      - max_weekly_hours
      - membership
      - organization
      - updated_at
      - user
      - username
    OrganizationTeacherConfigurationCreate:
      type: object
      description: |-
        Giving an existing member of this academy their teaching terms.

        The caller names a ``user``, not a membership — the same shape
        ``OrganizationMembershipCreateSerializer`` uses, and for the same reason: the
        membership is then looked up *inside* the view's verified organization, so a
        membership id belonging to another academy is not something a request can even
        express. The academy is never in the payload.

        ``approved`` may be sent at creation. Unlike the global ``TeacherProfile``,
        which the admin approves after the fact, an academy adding a teacher it has
        already vetted should not have to make two calls; the default is still False.
      properties:
        user:
          type: integer
        max_weekly_hours:
          type: integer
          minimum: 1
        hourly_payout_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
        approved:
          type: boolean
          default: false
      required:
      - max_weekly_hours
      - user
    PaginatedAuditLogList:
      type: object
      required:
      - count
      - results
      properties:
        count:
          type: integer
          example: 123
        next:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=4
        previous:
          type: string
          nullable: true
          format: uri
          example: http://api.example.org/accounts/?page=2
        results:
          type: array
          items:
            $ref: '#/components/schemas/AuditLog'
    ParentLinkCreate:
      type: object
      description: |-
        A parent links themselves to a student using the student's signup code.

        The view resolves ``student_code`` to a user (404 when no student holds that
        code) and passes it to ``save(student=...)``.
      properties:
        student_code:
          type: string
          writeOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/LinkedStudent'
          readOnly: true
      required:
      - student
      - student_code
    PatchedAssessmentRubricUpdate:
      type: object
      description: |-
        Editing live configuration in place: rename, retire, append.

        This is the operation the historical-data rule is about. Nothing here touches
        an existing ``AssessmentScore`` — a renamed criterion keeps its old name on
        every assessment already submitted, because that name was snapshotted onto the
        score. Retiring a criterion stops *new* assessments scoring it and leaves old
        ones intact.
      properties:
        name:
          type: string
          maxLength: 80
        description:
          type: string
          maxLength: 2000
        active:
          type: boolean
        criteria:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentCriterionWrite'
    PatchedLevelUpdate:
      type: object
      description: |-
        Editing a level's description. Not its position, and not its track.

        ``order`` and ``track`` are both absent. Renumbering a ladder is a deliberate
        operation on the whole track rather than a field edit (``Level.clean()``
        refuses it), and moving a level to another track would move it to another
        academy — the same thing ``Track`` refuses for itself.
      properties:
        name:
          type: string
          maxLength: 80
        min_age:
          type: integer
          minimum: 1
          nullable: true
        group_eligible:
          type: boolean
    PatchedOrganizationMembershipUpdate:
      type: object
      description: |-
        Suspending, reactivating, or changing what a member may do here.

        Both fields are optional, so this is the one shape behind three operations —
        suspend, reactivate, re-role. Neither ``organization`` nor ``user`` is
        editable: moving a membership between tenants or between people is not a
        change to a relationship, it is a different relationship.

        ``role`` offers the assignable subset, so a PATCH cannot promote anyone to
        ``owner``; the owner's own row is refused earlier still, by
        ``permissions.OwnerMembershipIsProtected``.
      properties:
        role:
          $ref: '#/components/schemas/AssignableOrganizationRoleEnum'
        status:
          $ref: '#/components/schemas/MembershipStatusEnum'
    PatchedOrganizationTeacherConfigurationUpdate:
      type: object
      description: |-
        Changing what this academy asks of, and pays, one of its teachers.

        All three fields optional, so one shape covers approving a teacher, re-capping
        their week and re-rating their hour. ``membership`` is absent and not editable:
        moving these terms to a different person or a different academy is not a change
        to a relationship, it is a different relationship.
      properties:
        max_weekly_hours:
          type: integer
          minimum: 1
        hourly_payout_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          nullable: true
        approved:
          type: boolean
    PatchedStudentEnrollmentUpdate:
      type: object
      description: Updating a student's enrollment status.
      properties:
        status:
          $ref: '#/components/schemas/StudentEnrollmentUpdateStatusEnum'
        track_id:
          type: integer
          nullable: true
        level_id:
          type: integer
          nullable: true
    PatchedTeacherTrackUpdate:
      type: object
      description: |-
        Withdrawing or restoring a teacher's eligibility for a track.

        ``active`` only. Moving an assignment to a different teacher or a different
        track is not a change to a relationship, it is a different relationship — and
        the row is never deleted, so the record of what was once granted survives the
        way a suspended membership does.
      properties:
        active:
          type: boolean
    PatchedTrackWrite:
      type: object
      description: |-
        Creating or renaming one academy's track.

        ``organization`` is absent by design, not omitted for brevity: the academy is
        the view's verified route context, so there is no field for a caller to point
        at a tenant they do not administer. Nor is it editable afterwards —
        ``Track.clean()`` refuses to move a track between academies, because that
        would carry its levels, placements and teacher assignments with it.

        Slug collisions are reported on the ``slug`` field rather than left to the
        database, and the check is scoped: ``tajweed`` may be taken *here* and free
        everywhere else. The model's ``(organization, slug)`` constraint is still the
        backstop.
      properties:
        name:
          type: string
          maxLength: 80
        slug:
          type: string
          maxLength: 80
          pattern: ^[-a-zA-Z0-9_]+$
    PayoutGenerate:
      type: object
      description: |-
        The lead's generation request: a period, and optionally one teacher.

        ``teacher`` narrows a run to one person — useful when a rate was set late and
        only that teacher's sessions need picking up. Omitting it generates for the
        whole academy. Either way the run is idempotent, so narrowing is a convenience
        rather than a way to avoid duplicates.

        Only ``lead`` and ``sub`` accounts are offered, because nobody else can be a
        booking's teacher. A student id here is a 400 rather than an empty run.
        When scoped to an organization, only active teachers in that organization are accepted.
      properties:
        period_start:
          type: string
          format: date-time
        period_end:
          type: string
          format: date-time
        teacher:
          type: integer
          nullable: true
      required:
      - period_end
      - period_start
    PayoutSession:
      type: object
      description: |-
        The session a payout is for — enough to reconcile a total, no more.

        This is what the spec means by "the session references needed to understand
        how the total was produced": which student, which level, when, how long, and
        whether it was a group class. ``cohort`` being non-null is also the reader's
        explanation for why a busy group session appears once rather than six times.
      properties:
        id:
          type: integer
          readOnly: true
        student:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          readOnly: true
        level:
          type: string
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        start_time_utc:
          type: string
          format: date-time
          readOnly: true
          description: Stored UTC, always.
        duration_minutes:
          type: integer
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/BookingStatusEnum'
          readOnly: true
        status_display:
          type: string
          readOnly: true
        cohort:
          type: integer
          readOnly: true
          nullable: true
          description: Set when this booking is a seat in a group class rather than
            a 1:1 session. Its teacher, level and start must match the cohort's.
      required:
      - cohort
      - duration_minutes
      - id
      - level
      - start_time_utc
      - status
      - status_display
      - student
      - track
    PayoutStatusEnum:
      enum:
      - generated
      - finalized
      type: string
      description: |-
        * `generated` - Generated
        * `finalized` - Finalized
    PlacementAudioAccess:
      type: object
      description: |-
        The response of the audio-access endpoint: a URL with an expiry.

        Serializer rather than a hand-built dict so the field types reach the
        OpenAPI schema — a client needs to know the URL stops working, and
        ``expires_at`` is the only thing telling it that.
      properties:
        url:
          type: string
          format: uri
          readOnly: true
          description: 'Short-lived private access to the recitation sample. Treat
            it as a secret: whoever holds it can fetch the file until it expires.'
        expires_at:
          type: string
          format: date-time
          readOnly: true
          description: UTC instant after which the URL stops working.
        expires_in:
          type: integer
          readOnly: true
          description: Seconds of remaining validity when issued.
      required:
      - expires_at
      - expires_in
      - url
    PlacementResult:
      type: object
      description: |-
        Read representation, shared by the submit, queue, 'mine' and children endpoints.

        Carries no URL for the audio. ``has_audio_sample`` is what a client branches
        on, ``audio_filename`` is what it shows in a list, and a signed URL comes
        from the dedicated endpoint when someone actually presses play.

        ``organization`` is the placement's own academy, read through its track. It is
        the only academy identifier in the payload — the spec forbids exposing
        unrelated ones, and there is nowhere here for one to come from.
      properties:
        id:
          type: integer
          readOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/PlacementStudent'
          readOnly: true
        organization:
          type: integer
          description: The owning academy's id, without loading the organization row.
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        has_audio_sample:
          type: boolean
          readOnly: true
        audio_filename:
          type: string
          nullable: true
          description: |-
            The sample's filename, without the storage path that holds it.

            The basename only. The full key is an implementation detail of the
            private bucket, and publishing it would put the one piece of the storage
            layout a client has no use for into every response.
          readOnly: true
        skipped_as_beginner:
          type: boolean
          readOnly: true
          description: Student declared themselves a complete beginner instead of
            recording. Mutually exclusive with audio_sample.
        recommended_level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
        reviewed_by:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          readOnly: true
        reviewed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        reviewed_at_local:
          type: string
          nullable: true
          description: reviewed_at (stored UTC) in the requesting user's own timezone.
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/PlacementResultStatusEnum'
          readOnly: true
          description: |-
            Recomputed from reviewed_at on every save(); never set by a client.

            * `pending` - Pending review
            * `reviewed` - Reviewed
      required:
      - audio_filename
      - has_audio_sample
      - id
      - organization
      - recommended_level
      - reviewed_at
      - reviewed_at_local
      - reviewed_by
      - skipped_as_beginner
      - status
      - student
      - track
    PlacementResultStatusEnum:
      enum:
      - pending
      - reviewed
      type: string
      description: |-
        * `pending` - Pending review
        * `reviewed` - Reviewed
    PlacementReview:
      type: object
      description: |-
        The lead teacher sets the level; the model stamps who and when.

        Two narrowings, one inside the other. ``recommended_level`` is restricted to
        levels of *this academy's* tracks, and then to this placement's own track — the
        first is the tenant boundary, the second is the Phase 2 product rule, and
        neither substitutes for the other.
      properties:
        recommended_level:
          type: integer
      required:
      - recommended_level
    PlacementStudent:
      type: object
      description: The student as the review queue needs them — no signup_code, no
        sprawl.
      properties:
        id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        timezone:
          type: string
          readOnly: true
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
        is_minor:
          type: boolean
          readOnly: true
          description: Snapshot computed from date_of_birth at signup. Not recomputed
            on birthdays — see learnings.md.
      required:
      - first_name
      - id
      - is_minor
      - last_name
      - timezone
      - username
    PlacementSubmit:
      type: object
      description: |-
        A student submits a recitation sample, or declares themselves a beginner.

        Exactly one of the two. Rejected at this layer with a 400 so the client gets
        a field error rather than the model's invariant surfacing as a 500 — which
        is the same reason the Phase 6 upload rules are attached to the field here
        rather than only enforced in the model.

        SaaS Phase 3 narrowed ``track`` to the academy in the URL. A student who
        belongs to two academies submits to each through its own route, and a track id
        from the academy they are *not* addressing is simply not in the queryset.
      properties:
        track:
          type: integer
        audio_sample:
          type: string
          format: uri
          nullable: true
          description: 'Up to 15 MB, one of: .flac, .m4a, .mp3, .mp4, .oga, .ogg,
            .opus, .wav, .webm. The file''s actual contents are checked, not just
            its name and declared type.'
        skipped_as_beginner:
          type: boolean
          default: false
      required:
      - track
    PricingAgreement:
      type: object
      description: |-
        The lead's view: everything, including the private note.

        Used by the create response and by the per-student history listing, so a
        superseded row reads exactly like a live one apart from ``active``.
      properties:
        id:
          type: integer
          readOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/PricingStudent'
          readOnly: true
        level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        standard_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          description: What this level would normally cost, snapshotted when the agreement
            was made. Deliberately stored rather than derived, so changing the standard
            rate later cannot silently reprice an existing agreement.
        agreed_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          description: What was actually negotiated. May equal standard_rate.
        reason:
          allOf:
          - $ref: '#/components/schemas/ReasonEnum'
          readOnly: true
        reason_display:
          type: string
          readOnly: true
        approved_by:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          readOnly: true
        notes:
          type: string
          readOnly: true
          description: 'Private: what was actually agreed and why. Never exposed to
            the student or their parent — see serializers.py.'
        active:
          type: boolean
          readOnly: true
          default: true
          description: False once a later agreement for the same student and level
            supersedes this one. Superseded rows are kept, never deleted.
      required:
      - active
      - agreed_rate
      - approved_by
      - id
      - level
      - notes
      - reason
      - reason_display
      - standard_rate
      - student
      - track
    PricingAgreementCreate:
      type: object
      description: |-
        The lead records a rate. Creating one supersedes the previous one.

        ``approved_by`` is not a client field — it is the requesting lead, the same
        way ``PlacementResult.reviewed_by`` is stamped from the request rather than
        supplied. ``active`` is not a client field either: a new agreement is the live
        one by definition, and deactivating an old one is the model's job, not a flag
        a caller sets.
      properties:
        student:
          type: integer
        level:
          type: integer
        standard_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          description: What this level would normally cost, snapshotted when the agreement
            was made. Deliberately stored rather than derived, so changing the standard
            rate later cannot silently reprice an existing agreement.
        agreed_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          description: What was actually negotiated. May equal standard_rate.
        reason:
          $ref: '#/components/schemas/ReasonEnum'
        notes:
          type: string
          description: 'Private: what was actually agreed and why. Never exposed to
            the student or their parent — see serializers.py.'
      required:
      - agreed_rate
      - level
      - reason
      - standard_rate
      - student
    PricingStudent:
      type: object
      description: The student as a pricing row needs them — no signup_code, no sprawl.
      properties:
        id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        timezone:
          type: string
          readOnly: true
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
      required:
      - first_name
      - id
      - last_name
      - timezone
      - username
    ProgressSnapshot:
      type: object
      description: The lead's view of a snapshot, including who generated it.
      properties:
        id:
          type: integer
          readOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/AssessmentParty'
          readOnly: true
        track:
          allOf:
          - $ref: '#/components/schemas/TrackBrief'
          readOnly: true
        period_start:
          type: string
          format: date-time
          readOnly: true
          description: Inclusive. Stored UTC.
        period_end:
          type: string
          format: date-time
          readOnly: true
          description: Exclusive. Stored UTC.
        generated_at:
          type: string
          format: date-time
          readOnly: true
        completed_sessions:
          type: integer
          readOnly: true
          description: Sessions taught in the period, assessed or not.
        assessed_sessions:
          type: integer
          readOnly: true
          description: How many of those carry an assessment. Never inflated to match.
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: 'Null when nothing in the period was assessed. Deliberately
            nullable: ''no assessments'' is missing data, and 0.00 would read as a
            score of zero out of five.'
        criterion_averages:
          readOnly: true
          description: 'Snapshot of the per-criterion averages: [{criterion_id, criterion_name,
            average, score_count}].'
        summary:
          type: string
          readOnly: true
          description: Short human line. Auto-composed from the numbers unless the
            lead supplies one.
        generated_by:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          readOnly: true
        visible_to_family:
          type: boolean
          readOnly: true
          description: Off until the lead publishes it. A family reads only published
            snapshots.
      required:
      - assessed_sessions
      - completed_sessions
      - criterion_averages
      - generated_at
      - generated_by
      - id
      - overall_average
      - period_end
      - period_start
      - student
      - summary
      - track
      - visible_to_family
    ProgressSnapshotCreate:
      type: object
      description: |-
        The lead generates a snapshot for one student, track and period.

        Every number in the result is computed here, not supplied: a snapshot a caller
        could hand values to would not be evidence of anything. Repeating the same
        request returns the existing row and a 200 rather than creating a second one.
      properties:
        student:
          type: integer
          description: Must be a student account; the model enforces the role.
        track:
          type: integer
        period_start:
          type: string
          format: date-time
        period_end:
          type: string
          format: date-time
        summary:
          type: string
          maxLength: 2000
        visible_to_family:
          type: boolean
          default: false
      required:
      - period_end
      - period_start
      - student
      - track
    ReasonEnum:
      enum:
      - discount_hardship
      - premium_direct
      - sibling_discount
      - standard
      type: string
      description: |-
        * `discount_hardship` - Discount — financial hardship
        * `premium_direct` - Premium — chose the lead teacher directly
        * `sibling_discount` - Sibling discount
        * `standard` - Standard — no negotiation
    RecentSummary:
      type: object
      description: A teacher summary as a family reads it — the one free-text field
        they see.
      properties:
        assessment_id:
          type: integer
          readOnly: true
        taught_at:
          type: string
          format: date-time
          readOnly: true
        assessed_at:
          type: string
          format: date-time
          readOnly: true
        teacher_summary:
          type: string
          readOnly: true
      required:
      - assessed_at
      - assessment_id
      - taught_at
      - teacher_summary
    Register:
      type: object
      properties:
        username:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          pattern: ^[\w.@+-]+$
          maxLength: 150
        email:
          type: string
          format: email
          title: Email address
          maxLength: 254
        password:
          type: string
          writeOnly: true
        first_name:
          type: string
          maxLength: 150
        last_name:
          type: string
          maxLength: 150
        role:
          $ref: '#/components/schemas/SelfRegisterableRoleEnum'
        timezone:
          type: string
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
          maxLength: 64
        date_of_birth:
          type: string
          format: date
          nullable: true
      required:
      - email
      - password
      - role
      - timezone
      - username
    RestAuthDetail:
      type: object
      properties:
        detail:
          type: string
          readOnly: true
      required:
      - detail
    RoleEnum:
      enum:
      - lead
      - sub
      - student
      - parent
      type: string
      description: |-
        * `lead` - Lead teacher
        * `sub` - Sub teacher
        * `student` - Student
        * `parent` - Parent
    RouteRequest:
      type: object
      description: |-
        ``POST /route/`` — let the system decide who teaches this.

        ...unless ``preferred_teacher`` is given, which is Phase 5's addition: naming
        a teacher turns off the routing order entirely and asks about that person
        alone. It is optional and additive, so every Phase 4 client keeps working
        unchanged — and a request that omits it is still refused a smuggled
        ``teacher`` key, because auto-routing must never quietly honour a preference
        it was not asked to honour.
      properties:
        level:
          type: integer
        requested_time_window:
          $ref: '#/components/schemas/TimeWindow'
        student:
          type: integer
          description: Required when a parent requests; ignored when a student does.
        preferred_teacher:
          type: integer
          nullable: true
          description: 'Ask for one teacher by name. Skips cohort/lead/sub resolution
            entirely: books that teacher if they can take it, otherwise puts the student
            on that teacher''s waitlist rather than assigning anybody else. Always
            a 1:1 session, never a cohort seat.'
      required:
      - level
      - requested_time_window
    Routed:
      type: object
      description: What routing decided. Read-only; assembled from a ``routing.Routed``.
      properties:
        routed:
          type: boolean
        routed_reason:
          type: string
        booking:
          $ref: '#/components/schemas/Booking'
        cohort:
          allOf:
          - $ref: '#/components/schemas/Cohort'
          nullable: true
      required:
      - booking
      - cohort
      - routed
      - routed_reason
    RoutedReasonEnum:
      enum:
      - lead_available
      - lead_full_routed
      - student_choice
      - cohort_assigned
      type: string
      description: |-
        * `lead_available` - Routed to the lead teacher, who had capacity
        * `lead_full_routed` - Lead teacher full, routed to a sub teacher
        * `student_choice` - Teacher named directly by the student or parent
        * `cohort_assigned` - Assigned a seat in an open cohort
    SelfRegisterableRoleEnum:
      enum:
      - sub
      - student
      - parent
      type: string
      description: |-
        * `sub` - sub
        * `student` - student
        * `parent` - parent
    SessionAssessmentCreate:
      type: object
      description: |-
        A teacher submits one completed session's assessment.

        ``booking`` and ``assessed_by`` are not client fields. The booking comes from
        the URL, scoped by the view to sessions this teacher taught; the assessor is
        the requesting user, stamped the same way ``PlacementResult.reviewed_by`` and
        ``PricingAgreement.approved_by`` are. A teacher naming somebody else as the
        assessor is not a request this API can express.
      properties:
        scores:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentScoreWrite'
        teacher_summary:
          type: string
          maxLength: 4000
        flagged_for_review:
          type: boolean
          default: false
        flag_reason:
          type: string
          maxLength: 2000
      required:
      - scores
    SkippedBooking:
      type: object
      description: |-
        One booking generation looked at and did not pay, and why.

        Reported rather than swallowed: ``no_payout_rate`` on a sub-teacher's session
        is the lead's cue to set a rate and run again, and it must not be
        indistinguishable from a quiet period.
      properties:
        booking_id:
          type: integer
          readOnly: true
        teacher_id:
          type: integer
          readOnly: true
        reason:
          type: string
          readOnly: true
      required:
      - booking_id
      - reason
      - teacher_id
    Statement:
      type: object
      description: |-
        The lead's view of any teacher's statement: the same totals, plus whose.

        ``teacher`` is a plain ``CharField`` over the username rather than a related
        field: a statement is a computed dataclass, not a model instance, so there is
        no model for a relational field to resolve against.
      properties:
        period_start:
          type: string
          format: date-time
          readOnly: true
        period_end:
          type: string
          format: date-time
          readOnly: true
        session_count:
          type: integer
          readOnly: true
        finalized_count:
          type: integer
          readOnly: true
        total_amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,10}(?:\.\d{0,2})?$
          readOnly: true
        currency:
          type: string
          readOnly: true
        status:
          allOf:
          - $ref: '#/components/schemas/StatementStatusEnum'
          readOnly: true
        payouts:
          type: array
          items:
            $ref: '#/components/schemas/TeacherPayout'
          readOnly: true
        teacher:
          type: string
          readOnly: true
      required:
      - currency
      - finalized_count
      - payouts
      - period_end
      - period_start
      - session_count
      - status
      - teacher
      - total_amount
    StatementStatusEnum:
      enum:
      - empty
      - generated
      - partly_finalized
      - finalized
      type: string
      description: |-
        * `empty` - No payout records in this period
        * `generated` - Generated, none finalized
        * `partly_finalized` - Partly finalized
        * `finalized` - Fully finalized
    StudentDetail:
      type: object
      description: A student's enrollment, as seen in the academy's detail view.
      properties:
        id:
          type: integer
          readOnly: true
        user_id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
        email:
          type: string
          readOnly: true
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        date_of_birth:
          type: string
          format: date
          readOnly: true
        is_minor:
          type: boolean
          readOnly: true
        enrollment_status:
          type: string
          readOnly: true
        track_id:
          type: integer
          readOnly: true
          nullable: true
        level_id:
          type: integer
          readOnly: true
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - date_of_birth
      - email
      - enrollment_status
      - first_name
      - id
      - is_minor
      - last_name
      - level_id
      - track_id
      - updated_at
      - user_id
      - username
    StudentEnrollmentCreate:
      type: object
      description: Enrolling an existing student user into an academy.
      properties:
        user:
          type: integer
        track_id:
          type: integer
          nullable: true
        level_id:
          type: integer
          nullable: true
      required:
      - user
    StudentEnrollmentUpdateStatusEnum:
      enum:
      - active
      - inactive
      type: string
      description: |-
        * `active` - Active
        * `inactive` - Inactive
    StudentList:
      type: object
      description: A student's enrollment, as seen in the academy's list.
      properties:
        id:
          type: integer
          readOnly: true
        user_id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
        email:
          type: string
          readOnly: true
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        date_of_birth:
          type: string
          format: date
          readOnly: true
        is_minor:
          type: boolean
          readOnly: true
        enrollment_status:
          type: string
          readOnly: true
        track_id:
          type: integer
          readOnly: true
          nullable: true
        level_id:
          type: integer
          readOnly: true
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - date_of_birth
      - email
      - enrollment_status
      - first_name
      - id
      - is_minor
      - last_name
      - level_id
      - track_id
      - updated_at
      - user_id
      - username
    StudentProgress:
      type: object
      description: |-
        Progress in one track, for the student or their linked parent.

        ``overall_average`` and every criterion average are nullable, and null means
        "nothing assessed in this period" rather than "scored zero". ``recommended_level``
        is read from Phase 2's placement result and is never written by this app.
      properties:
        track:
          allOf:
          - $ref: '#/components/schemas/TrackBrief'
          readOnly: true
        recommended_level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
          nullable: true
        period_start:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        period_end:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        completed_sessions:
          type: integer
          readOnly: true
        assessed_sessions:
          type: integer
          readOnly: true
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        criterion_averages:
          type: array
          items:
            $ref: '#/components/schemas/CriterionAverage'
          readOnly: true
        last_assessed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
        recent_summaries:
          type: array
          items:
            $ref: '#/components/schemas/RecentSummary'
          readOnly: true
      required:
      - assessed_sessions
      - completed_sessions
      - criterion_averages
      - last_assessed_at
      - overall_average
      - period_end
      - period_start
      - recent_summaries
      - recommended_level
      - track
    TeacherAssessment:
      type: object
      description: |-
        The assessing teacher's view of their own submission.

        Carries their own flag and its reason — they wrote both — and
        ``lead_reviewed_at`` so they can see the lead has looked. It does **not**
        carry ``lead_review_note`` or ``lead_reviewed_by``: the spec's visibility
        section gives a sub-teacher no access to lead review notes in this phase, and
        widening that is a decision, not a serializer tweak.
      properties:
        id:
          type: integer
          readOnly: true
        booking:
          allOf:
          - $ref: '#/components/schemas/AssessmentBooking'
          readOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/AssessmentParty'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        assessed_at:
          type: string
          format: date-time
          readOnly: true
          description: When it was submitted. Stored UTC, rendered per-viewer.
        assessed_at_local:
          type: string
          nullable: true
          readOnly: true
        rubric_name:
          type: string
          readOnly: true
          description: Snapshot of the rubric's name at submission time.
        criteria_snapshot:
          readOnly: true
          description: 'Snapshot of the criteria scored: [{id, name, order}]. What
            the teacher was actually looking at, frozen.'
        scores:
          type: array
          items:
            $ref: '#/components/schemas/AssessmentScore'
          readOnly: true
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
        teacher_summary:
          type: string
          readOnly: true
          description: 'Written for the family: this is the one free-text field they
            read.'
        flagged_for_review:
          type: boolean
          readOnly: true
          description: The teacher asking the lead to look at this session.
        flag_reason:
          type: string
          readOnly: true
          description: Why it was flagged. Internal — never shown to a student or
            parent.
        lead_reviewed_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Set by an explicit lead action. Viewing an assessment does
            not set it.
      required:
      - assessed_at
      - assessed_at_local
      - booking
      - criteria_snapshot
      - flag_reason
      - flagged_for_review
      - id
      - lead_reviewed_at
      - overall_average
      - rubric_name
      - scores
      - student
      - teacher_summary
      - track
    TeacherPayout:
      type: object
      description: 'The lead''s view: the same record, plus whose it is.'
      properties:
        id:
          type: integer
          readOnly: true
        teacher:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          readOnly: true
        booking:
          allOf:
          - $ref: '#/components/schemas/PayoutSession'
          readOnly: true
        cohort:
          type: integer
          readOnly: true
          nullable: true
          description: Set when the paid session is a group class, so the unique constraint
            below can enforce one payout per cohort session. Always equal to booking.cohort.
        minutes_paid:
          type: integer
          readOnly: true
          description: Session length used in the calculation, snapshotted from the
            booking. Stored rather than derived so the record explains its own amount
            without depending on a row that could later be corrected.
        rate_used:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          description: 'The teacher''s hourly rate at generation time, snapshotted
            the same way PricingAgreement.standard_rate is: changing the live rate
            must not reprice a payout that has already been made.'
        amount:
          type: string
          format: decimal
          pattern: ^-?\d{0,8}(?:\.\d{0,2})?$
          readOnly: true
          description: minutes_paid / 60 × rate_used, rounded once. See payout_amount().
        currency:
          type: string
          readOnly: true
          description: ISO code, snapshotted. NGN today; conversion is out of scope.
        status:
          allOf:
          - $ref: '#/components/schemas/PayoutStatusEnum'
          readOnly: true
          description: |-
            generated → finalized. Finalized records are immutable.

            * `generated` - Generated
            * `finalized` - Finalized
        status_display:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        finalized_at:
          type: string
          format: date-time
          readOnly: true
          nullable: true
          description: Stamped when the lead finalizes. Null while generated.
      required:
      - amount
      - booking
      - cohort
      - created_at
      - currency
      - finalized_at
      - id
      - minutes_paid
      - rate_used
      - status
      - status_display
      - teacher
    TeacherProfile:
      type: object
      description: Read-only in this phase; profiles are created via the admin.
      properties:
        bio:
          type: string
          readOnly: true
        max_weekly_hours:
          type: integer
          readOnly: true
          description: Capacity dial used by scheduling in a later phase.
        hourly_payout_rate:
          type: string
          format: decimal
          pattern: ^-?\d{0,6}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
          description: Null for the lead teacher, who is not paid a per-hour rate.
        is_lead:
          type: boolean
          readOnly: true
        approved:
          type: boolean
          readOnly: true
          description: A sub-teacher is not bookable until a lead flips this true.
      required:
      - approved
      - bio
      - hourly_payout_rate
      - is_lead
      - max_weekly_hours
    TeacherReport:
      type: object
      description: |-
        One teacher's row in the lead's quality report.

        Deliberately not sortable by score and deliberately without a rank field. The
        rows arrive ordered by username; a client that wants a league table has to
        build one itself, and the phase spec says not to.
      properties:
        teacher:
          allOf:
          - $ref: '#/components/schemas/AssessmentParty'
          readOnly: true
        assessed_sessions:
          type: integer
          readOnly: true
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
        by_track:
          type: array
          items:
            $ref: '#/components/schemas/TeacherTrackReport'
          readOnly: true
        flagged:
          type: integer
          readOnly: true
        awaiting_lead_review:
          type: integer
          readOnly: true
      required:
      - assessed_sessions
      - awaiting_lead_review
      - by_track
      - flagged
      - overall_average
      - teacher
    TeacherTrack:
      type: object
      description: |-
        One teacher's eligibility for one track, inside one academy.

        ``user`` and ``organization`` are read off the membership rather than stored
        twice, which is the same shape ``accounts.OrganizationTeacherConfiguration``
        exposes and for the same reason: the membership is the only place the pair is
        written. ``username`` because an id does not identify a person to the human
        deciding what they may teach.
      properties:
        id:
          type: integer
          readOnly: true
        membership:
          type: integer
          readOnly: true
          description: The academy-and-teacher relationship this eligibility is for.
        user:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
        organization:
          type: integer
          readOnly: true
        track:
          type: integer
          readOnly: true
          description: A track owned by the same academy as the membership.
        track_slug:
          type: string
          readOnly: true
        active:
          type: boolean
          readOnly: true
          description: Whether the teacher may currently be assigned this track here.
            Withdrawing eligibility keeps the row, the way a suspended membership
            does, so the record of what was once granted survives.
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - active
      - created_at
      - id
      - membership
      - organization
      - track
      - track_slug
      - updated_at
      - user
      - username
    TeacherTrackCreate:
      type: object
      description: |-
        Giving one of this academy's teachers a track to teach.

        The caller names a ``user``, not a membership — the shape
        ``accounts.OrganizationTeacherConfigurationCreateSerializer`` established, and
        for the same reason: the membership is then looked up *inside* the view's
        verified organization, so a membership belonging to another academy is not
        something a request can express. An academy administrator therefore cannot
        reach a teacher's eligibility in a different academy even when they know both
        ids, which is the phase spec's cross-academy teacher requirement.

        ``track`` is narrowed to this academy for the same reason.
      properties:
        user:
          type: integer
        track:
          type: integer
        active:
          type: boolean
          default: true
      required:
      - track
      - user
    TeacherTrackReport:
      type: object
      properties:
        track:
          allOf:
          - $ref: '#/components/schemas/TrackBrief'
          readOnly: true
        assessed_sessions:
          type: integer
          readOnly: true
        overall_average:
          type: string
          format: decimal
          pattern: ^-?\d{0,1}(?:\.\d{0,2})?$
          readOnly: true
          nullable: true
      required:
      - assessed_sessions
      - overall_average
      - track
    TimeWindow:
      type: object
      description: |-
        The slot a requested session would occupy: when it starts, how long it runs.

        The Phase 4 spec names the field ``requested_time_window`` without defining
        its shape. This is the exact-slot reading (product owner's call, 2026-08-24):
        routing decides *who* teaches, never *when*, so a student is never quietly
        moved to a different time than the one they asked for. The single exception is
        a cohort, whose own ``schedule_start_utc`` wins within
        ``COHORT_START_TOLERANCE`` — and the response says so, in ``routed_reason``.
      properties:
        start_time_utc:
          type: string
          format: date-time
        duration_minutes:
          type: integer
          minimum: 1
          default: 30
      required:
      - start_time_utc
    Token:
      type: object
      description: Serializer for Token model.
      properties:
        key:
          type: string
          maxLength: 40
      required:
      - key
    Track:
      type: object
      description: |-
        One academy's track, with its levels nested in order.

        ``organization`` travels in the body even though it is already in the URL. The
        future frontend holds tracks from several academies in one client — a teacher
        working for two of them — and a track that does not say whose it is has to be
        remembered rather than read.
      properties:
        id:
          type: integer
          readOnly: true
        organization:
          type: integer
          readOnly: true
          description: The academy that owns this track, and through it the track's
            levels and placements. Set at creation; not editable afterwards.
        name:
          type: string
          readOnly: true
        slug:
          type: string
          readOnly: true
          description: Unique within the owning academy, not across the platform.
          pattern: ^[-a-zA-Z0-9_]+$
        levels:
          type: array
          items:
            $ref: '#/components/schemas/Level'
          readOnly: true
      required:
      - id
      - levels
      - name
      - organization
      - slug
    TrackBrief:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        name:
          type: string
          readOnly: true
        slug:
          type: string
          readOnly: true
          description: Unique within the owning academy, not across the platform.
          pattern: ^[-a-zA-Z0-9_]+$
      required:
      - id
      - name
      - slug
    TrackWrite:
      type: object
      description: |-
        Creating or renaming one academy's track.

        ``organization`` is absent by design, not omitted for brevity: the academy is
        the view's verified route context, so there is no field for a caller to point
        at a tenant they do not administer. Nor is it editable afterwards —
        ``Track.clean()`` refuses to move a track between academies, because that
        would carry its levels, placements and teacher assignments with it.

        Slug collisions are reported on the ``slug`` field rather than left to the
        database, and the check is scoped: ``tajweed`` may be taken *here* and free
        everywhere else. The model's ``(organization, slug)`` constraint is still the
        backstop.
      properties:
        name:
          type: string
          maxLength: 80
        slug:
          type: string
          maxLength: 80
          pattern: ^[-a-zA-Z0-9_]+$
      required:
      - name
      - slug
    User:
      type: object
      description: The requesting user's own profile.
      properties:
        id:
          type: integer
          readOnly: true
        username:
          type: string
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
          pattern: ^[\w.@+-]+$
          maxLength: 150
        email:
          title: Email address
          oneOf:
          - type: string
            format: email
            maxLength: 254
          - type: string
            maxLength: 0
        first_name:
          type: string
          maxLength: 150
        last_name:
          type: string
          maxLength: 150
        role:
          allOf:
          - $ref: '#/components/schemas/RoleEnum'
          readOnly: true
        timezone:
          type: string
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
          maxLength: 64
        date_of_birth:
          type: string
          format: date
          nullable: true
        is_minor:
          type: boolean
          readOnly: true
          description: Snapshot computed from date_of_birth at signup. Not recomputed
            on birthdays — see learnings.md.
        is_fully_active:
          type: boolean
          readOnly: true
        signup_code:
          type: string
          readOnly: true
          description: Opaque code a student shares with a parent so the parent can
            link to them. Generated automatically at signup.
        teacher_profile:
          allOf:
          - $ref: '#/components/schemas/TeacherProfile'
          readOnly: true
        date_joined:
          type: string
          format: date-time
          readOnly: true
        date_joined_local:
          type: string
          description: date_joined rendered in the user's own timezone (stored as
            UTC).
          readOnly: true
      required:
      - date_joined
      - date_joined_local
      - id
      - is_fully_active
      - is_minor
      - role
      - signup_code
      - teacher_profile
      - timezone
      - username
    WaitlistEntry:
      type: object
      description: |-
        Read representation of one waitlist entry, for whatever side is looking.

        ``requested_end_utc`` is derived from the start and duration — convenience,
        not another stored value. ``fulfilled_booking`` is published as the booking
        id when set; the full booking payload is not, because the interested party
        (the lead, the family) can read the session itself through the booking
        endpoint.
      properties:
        id:
          type: integer
          readOnly: true
        student:
          allOf:
          - $ref: '#/components/schemas/WaitlistParty'
          readOnly: true
        requested_teacher:
          allOf:
          - $ref: '#/components/schemas/WaitlistParty'
          readOnly: true
        level:
          allOf:
          - $ref: '#/components/schemas/Level'
          readOnly: true
        track:
          type: string
          description: Unique within the owning academy, not across the platform.
          readOnly: true
        requested_start_utc:
          type: string
          format: date-time
          readOnly: true
          description: The slot the family asked for, stored UTC. Not in the spec's
            field list — added with explicit approval so a promotion knows what time
            to offer.
        requested_end_utc:
          type: string
          format: date-time
          readOnly: true
        requested_start_local:
          type: string
          nullable: true
          description: The slot asked for, in the requesting user's zone.
          readOnly: true
        requested_duration_minutes:
          type: integer
          readOnly: true
          description: Length of the session asked for. Added alongside the start.
        requested_at:
          type: string
          format: date-time
          readOnly: true
        requested_at_local:
          type: string
          nullable: true
          description: requested_at (stored UTC) in the requester's own zone.
          readOnly: true
        priority:
          type: integer
          readOnly: true
          description: Set by hand, never computed. The product owner's call (2026-08-25)
            on this phase's open question was that a premium_direct pricing agreement
            does *not* buy queue position, so nothing in the code writes this field
            — a lead raises it deliberately or not at all.
        notified:
          type: boolean
          readOnly: true
          description: 'Whether the family has been told a slot opened. Nothing in
            this phase sets it: automatic notification is explicitly out of scope
            (see tech-debt.md), so it is here for the phase that delivers it.'
        fulfilled_booking:
          type: integer
          readOnly: true
          nullable: true
          description: Set when this entry is promoted into a real session. The entry
            then stays as a record rather than being deleted.
        status:
          type: string
          description: '``open`` or ``fulfilled`` — the one thing a parent asks for.'
          readOnly: true
      required:
      - fulfilled_booking
      - id
      - level
      - notified
      - priority
      - requested_at
      - requested_at_local
      - requested_duration_minutes
      - requested_end_utc
      - requested_start_local
      - requested_start_utc
      - requested_teacher
      - status
      - student
      - track
    WaitlistParty:
      type: object
      description: A person in a waitlist row — no signup_code, no sprawl, same as
        bookings.
      properties:
        id:
          type: integer
          readOnly: true
        username:
          type: string
          readOnly: true
          description: Required. 150 characters or fewer. Letters, digits and @/./+/-/_
            only.
        first_name:
          type: string
          readOnly: true
        last_name:
          type: string
          readOnly: true
        role:
          allOf:
          - $ref: '#/components/schemas/RoleEnum'
          readOnly: true
        timezone:
          type: string
          readOnly: true
          description: IANA zone name, e.g. 'Africa/Lagos'. Required at signup.
      required:
      - first_name
      - id
      - last_name
      - role
      - timezone
      - username
    WaitlistPromote:
      type: object
      description: |-
        The lead promotes an entry into a session.

        The slot is optional: it defaults to the one the family asked for, and
        overrides only *when* the session runs — the entry always keeps its original
        request on record. ``duration_minutes`` defaults the same way.
      properties:
        start_time_utc:
          type: string
          format: date-time
        duration_minutes:
          type: integer
          minimum: 1
    WeekdayEnum:
      enum:
      - 0
      - 1
      - 2
      - 3
      - 4
      - 5
      - 6
      type: integer
      description: |-
        * `0` - Monday
        * `1` - Tuesday
        * `2` - Wednesday
        * `3` - Thursday
        * `4` - Friday
        * `5` - Saturday
        * `6` - Sunday
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: sessionid
    tokenAuth:
      type: apiKey
      in: header
      name: Authorization
      description: Token-based authentication with required prefix "Token"
